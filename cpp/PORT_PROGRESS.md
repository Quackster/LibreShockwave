# LibreShockwave C++ Port Progress

Objective: port LibreShockwave's non-editor runtime/player stack to C++ while keeping the C++ tree buildable and tested after each slice. The editor is out of scope for the current port objective.

Current requested goal, after the completed Habbo browser/networking proof: stop treating the Habbo movie as a product-specific target and continue the C++ non-editor runtime/player port. The active workstream is C++ cleanup and parity: keep Java/Gradle source and project material out of the active tree, keep Habbo/movie-specific network debug hooks and fixture-specific variable seeding out of reusable runtime/browser assets, maintain the README as C++ source documentation, and continue generic fixture verification against `/var/www/html/dcr0910` and `/opt/git/v1_assets/`.

## Current Status

C++-first cleanup is complete in the active tree. The Java/Gradle project served as the historical implementation reference for behavior already ported to C++; the repository-facing source now centers on the CMake/C++ library, native tools, probes, and Emscripten browser player. Required runtime font assets were preserved under `cpp/resources/fonts`, movie-specific browser debugging hooks used only for the Habbo networking proof were removed from reusable web assets, and `README.md` now documents the C++ source and build flow. Existing editor scaffolding is excluded from the default CMake build and is not being advanced as part of the current port objective.

Shockwave3D/W3D can be ignored for the current C++ parity objective because it was never completed in the existing LibreShockwave Java runtime. Existing C++ W3D parsing and preliminary bake work is useful opportunistic coverage, but deeper W3D renderer parity is not required until after the Java-equivalent non-editor runtime/player behavior is complete.

This file tracks the current state and remaining work only. Detailed chronological changes live in git.

## Current Focus

The Habbo browser movie load was a validation proof, not an ongoing product-specific target. That proof is complete: the C++ WASM browser player can load `http://localhost/dcr0910/loader.dcr`, render the expected Habbo login splash, and exercise the WebSocket -> JS bridge plus C++ SMUS networking path.

The active goal now is the C++-first cleanup and parity work: keep the repository centered on the C++ runtime/player source, remove Java source and Gradle-era project material from the active tree, remove hardcoded Habbo/movie-specific debugging references and fixture-specific network-variable hooks, and maintain a README that documents the C++ source, CMake build, native probes, WASM build, and generic browser verification flow. Future movie fixtures, including `/opt/git/v1_assets/` and `/var/www/html/dcr0910`, should remain verification inputs rather than hardcoded runtime behavior.

Current implementation work is continuing in small tested C++ runtime/player slices after that cleanup, with the current slice focused on sound-channel state parity. Java/Gradle and editor work remain out of scope.

## Current Cleanup Result

- Java source modules, Gradle wrapper/build files, and old Java/TeaVM CI workflows are removed from the active tree.
- Bundled font assets needed by the C++ runtime moved from the old Java module into `cpp/resources/fonts`; CMake and C++ tests now use the C++ resource path.
- Reusable C++ browser assets no longer expose the Habbo fixture-only debug self-test that seeded one movie's globals or called one movie's handlers such as `Logon`, `sendFuseMsg`, or `DefaultMessageHandler`.
- Default CMake builds are runtime/player-only. Editor model sources and the GTK shell are quarantined behind the explicit `LIBRESHOCKWAVE_BUILD_EDITOR` option and are not part of the default library, probes, tests, or documented runtime dependencies.
- `README.md` now documents the C++ project: CMake requirements, native tools, fixture probes, Emscripten/WASM build, browser serving instructions, and current runtime scope.

## Current C++ Coverage

- Build: root CMake integration, `LibreShockwave::libreshockwave`, CTest coverage, optional zlib support, and default runtime/player-only source selection.
- Core file parsing: endian-aware binary IO, FourCC/MoaID helpers, Director chunk metadata, RIFX/XFIR loading including little-endian XFIR memory maps, D3 RIFF loading, Afterburner map/decompression support, lazy chunk reparse, and chunk categorization.
- Director chunks and metadata: config, palette, bitmap, sound/media, text, font maps, cast lists, cast members, key tables, score, large sparse score frame data, frame labels, script names, script contexts, and script bytecode.
- Cast/media SDK: cast-member wrappers, cast-library lookup, key-table linked cast-member chunk/media discovery including Shockwave3D member `XMED` payload resolution into `W3DFile`, palette resolution, bitmap decoding, bitmap drawing/color/ink helpers, sound conversion, W3D entry/node/texture/resource-ref parsing plus named mesh/material/resource lookups, node-to-mesh/material/texture/resource binding helpers, renderable W3D mesh packet assembly with local/world bounds, scene graph child lookup and parent-composed world transforms, mesh vertex/UV/face decoding, and material color/texture decoding, generated font decoding, bitmap font/PFR/TTF helpers, and utility formatting/string helpers.
- Lingo and VM foundations: datum model/formatting, opcode metadata, decompiler nodes/translation, value parsing, VM scope/execution context/runtime/safepoints/tracing/alert hooks/deferred dispatch, opcode registry handlers, builtin registry, and major builtins for math, strings, lists, output, control flow, constructors, types, casts, images, networking, sound including handled `beep`, top-level `_sound`, movie-backed `soundEnabled`, sound-channel `member` state, playlist state for `queue`/`setPlaylist`/`getPlaylist`/`playNext`, `loopCount` storage/default playback use, method-style sound-channel `pan`, stateful `pan`/`startTime`/`endTime`/`loopStartTime`/`loopEndTime`, and `_sound` `soundLevel`/`soundKeepDevice`/`soundMixMedia`, Xtras, and timeouts. List builtin coverage includes both global-function and method-style parity for key operations such as `findPos`, `getFirst`, `getLast`, `join`, and deep-copying `duplicate` over lists/property lists.
- Runtime/player foundations: player facade, builtin context, VM event dispatch, sprite and movie properties including mutable sound/stage/layout flags, modern sprite `color`/`bgColor` getter aliases with color-object retention, sprite `left`/`top`/`right`/`bottom` edge setters, rectangle-based sprite `intersects`/`within` method and opcode checks, sprite `castLibNum` reassignment, numeric sprite member assignment retaining cast-library context, legacy sprite media/authoring property retention, constraint-aware location clamping, input-driven `moveableSprite` dragging, sprite-level `editableText` field editing, shape `lineSize`/`pattern` render overrides, and shape-member `filled`/`shapeType`/`lineSize`/`pattern`/geometry runtime render overrides, legacy input event-script and movie `timeOut` dispatch, media availability defaults, memory hints, time-since-input counters (`lastClick`, `lastEvent`, `lastKey`, `lastRoll`), legacy timeout/player color-depth flags, and runtime capability checks (`multiSound`, `netPresent`, `safePlayer`), score navigation, input state/handler, hit testing, cursor manager, sprite registry/state, behavior instances, event dispatching, frame context, timeout manager, sound manager with queued elapsed-time tracking, sound-enabled playback gating, and global sound-level volume scaling, net manager, queued browser-facing net/audio/JPEG/Multiuser bridges, default player text renderer wiring, and player-owned Xtra manager wiring.
- Rendering pipeline: stage renderer, sprite baker, render pipeline context/runner, software frame renderer, bitmap cache/ink processor, text renderer interface, simple text renderer, explicit Shockwave3D sprite classification, preliminary linked-W3D mesh-to-bitmap baking with transparent alpha output, basic material-alpha compositing, stable average-Z mesh draw ordering, decoded texture sampling with UV interpolation when a W3D texture provider supplies bitmap data, and verbose render-probe diagnostics for unbaked sprite/member types.
- Dynamic/runtime member support: runtime bitmap/text/palette/script payloads, dynamic member creation/reuse/erase, runtime member media copying, imported bitmap media, runtime registration points, editable field state/overlay helpers, and dynamic bitmap/text rendering through the player pipeline.
- Debug foundations: debugger data/control models, breakpoint/watch/expression helpers, lifecycle diagnostics, trace controls, expression evaluation, and runtime/player inspection support.
- Browser/WASM foundations: C++ WASM player wrapper, runtime bridge, prefixed native-testable C ABI export facade, browser-side export adapter for the worker's TeaVM-style method names, C++-owned browser bootstrap page/player/worker assets, self-checking adapter/header/export-list/browser-asset contract tests, and conditional Emscripten CMake packaging, covering movie loading, stale async load suppression, nonblank score-sprite frame rendering into the JS-facing RGBA buffer, browser worker-to-canvas RGBA blitting, optional SharedArrayBuffer/Atomics frame transport with transferable fallback, cursor/caret/selection overlay compositing, clipboard text selection commands, debug log flushing, trace controls, call-stack/sprite/text/bootstrap/runtime diagnostics, playback/input controls, exported script deadline wiring, navigation polling, external params, POST/fallback fetches, cross-origin main-thread fetch relay, browser image import decode, audio command forwarding with stale callback protection, Multiuser WebSocket bridging, embedded JPEG decode queues, host result delivery, and last-error retrieval.
- Fixture validation: native `libreshockwave_probe` scans Director `.cct`, `.cst`, `.dcr`, `.dir`, and `.dxr` files through C++ load, cast-member metadata, and score metadata paths while skipping extension-matched non-Director placeholder files, reporting processed-file progress, and optionally reporting the active file during long scans. Native `libreshockwave_render_probe` loads the same fixture set through the player renderer, preloads local external casts through the C++ player/net path, reports processed-file progress, produces frame-buffer statistics, can scan score frames for nonblank output, and can optionally run bounded `play()`/tick lifecycle dispatch before rendering. Native `libreshockwave_wasm_bridge_probe` drives the browser-facing C++ WASM C ABI exports against the same fixture set, satisfies pending host fetches from local sidecar files, renders through the exported RGBA buffer API, captures bounded playback errors, and scans active-stage frames by stepping the exported player. All three native probes default to `/var/html` when present and use `/var/www/html` as the local fixture mapping when `/var/html` is absent.

## Verification

- C++ coverage is verified through the `libreshockwave_cpp_tests` CTest executable.
- Current Lingo list-builtin parity is covered by CTest assertions for global `findPos` over ordinary lists, `getFirst`, `join`, and deep-copying `duplicate` over nested lists/property lists, plus the existing method-dispatcher assertions for the same list/property-list behavior.
- Current sound-channel parity is covered by CTest assertions for per-channel `member` state, playback-updated `member` state, playlist `queue`/`setPlaylist`/`getPlaylist`/`playNext`, `loopCount` defaults, clamping, property get/set through the Lingo sound-channel dispatcher, method-style `pan` get/set, use of stored `loopCount` on subsequent `play`, explicit play-argument `loopCount` overrides, and stateful round-tripping of `pan`, `startTime`, `endTime`, `loopStartTime`, and `loopEndTime`.
- Current cleanup verification passed: JS syntax checks for `cpp/web/libreshockwave-cpp-player.js`, `cpp/web/libreshockwave-cpp-worker.js`, and `cpp/tools/browser_fixture_check.js`; debug CMake configure/build of `libreshockwave_tests`, `libreshockwave_probe`, `libreshockwave_render_probe`, and `libreshockwave_wasm_bridge_probe`; default archive inspection confirming no editor objects and no default editor target; CTest; dcr0910 load/render/bridge native probes; Emscripten rebuild of `libreshockwave_cpp_wasm`; generic browser fixture checks for `/dcr0910/loader.dcr`, the full 21-movie `/var/www/html/dcr0910` set, and the v1 Director movie set with WebSocket and SMUS diagnostics; and `git diff --check`.
- Cast-member linked media coverage verifies `DirectorFile` can report key-table-linked chunk metadata and lazily load linked chunks for member-owned text, score, bitmap, and sound payloads; bitmap, text, score, sound, and PFR font paths now share that canonical lookup.
- W3D parser coverage includes flat resource parsing, scene-data detection, Director key-table `XMED` payload resolution for Shockwave3D members, node-to-resource binding helpers, renderable mesh packet assembly with local/world bounds, per-vertex UV decoding for the recognized mesh layout, scene graph child traversal, and parent-composed node/shape world transforms.
- Render pipeline coverage verifies Shockwave3D cast members are classified as `SHOCKWAVE_3D` sprites from both authored cast chunks and runtime cast members. File-backed Shockwave3D members with linked W3D mesh data now bake to 32-bit alpha bitmaps through the sprite baker, including diffuse material alpha, stable average-Z mesh ordering, and decoded texture sampling through interpolated UVs when a texture decode provider returns bitmap data, while runtime/no-media Shockwave3D members remain explicitly non-baked until fuller W3D runtime rendering is implemented.
- Browser bridge coverage includes native C ABI export tests that load a synthetic Director movie, start playback, render a shaped score sprite, verify exact RGBA bytes from the exported render buffer, verify exported script deadline controls, and keep the adapter's required exports synchronized with the C ABI header and Emscripten export list.
- C++ browser bootstrap coverage includes resource contract tests that verify the C++ web page/player/worker assets instantiate `libreshockwave-cpp-wasm.js`, use the exported C ABI for movie loading, stale async load suppression, local/POST/fallback fetch delivery, cross-origin main-thread fetch relay, browser image import decode, embedded JPEG decode, audio command forwarding, Multiuser WebSocket events, navigation polling, frame rendering, optional SharedArrayBuffer/Atomics frame transport, cursor/caret/selection overlays, clipboard text selection commands, debug log flushing, trace controls, call-stack/sprite/text/bootstrap/runtime diagnostics, script deadlines, input forwarding, and canvas blitting, and are copied by the Emscripten CMake target.
- Fixture coverage is verified with `libreshockwave_probe` against local Director fixture roots. `/var/www/html/dcr0910` currently passes with 64 files loaded, 0 skipped, 0 failed, 4,023,549 bytes, 18,718 chunks, 8,924 cast members, 8,924 scanned members, 883 scripts, 24,144 score channels, and 1,655 score frames. The no-argument default-root probe maps absent `/var/html` to `/var/www/html` and currently scans 6,091 candidates with 6,031 loaded, 60 skipped, 0 failed, 178,471,955 bytes, 356,882 chunks, 149,059 cast members, 149,059 scanned members, 3,676 scripts, 35,210 score channels, and 2,928 score frames.
- Player render fixture coverage is verified with `libreshockwave_render_probe`. The no-argument default-root static render probe maps absent `/var/html` to `/var/www/html` and currently scans 6,091 candidates with 6,031 rendered, 60 skipped, 0 failed, 430 external cast entries fetched/loaded from local sidecars, 12,976 collected sprites, 12,954 baked sprites, 10,706,800 pixels, 224,557 non-background pixels, and 0 script errors. The no-argument default-root 20-frame score-frame scan currently passes with 612 scanned frames, 576 frames with sprites, 564 frames with baked sprites, 435 nonblank frames, 23 files with nonblank scanned frames, and 0 script errors; verbose scan output currently has no non-empty genuine unbaked render-target fields after separating expected missing/script score references into `nonRenderable*` diagnostics. Bounded lifecycle render probing over `/var/www/html/dcr0910` currently passes with 64 files rendered, 0 skipped, 0 failed, 218 external cast entries fetched/loaded from local sidecars, 12,965 collected sprites, 12,957 baked sprites, 359,235 non-background pixels, and 0 script errors.
- The `/opt/git/v1_assets/projectorrays_lingo` reference asset root passes native load and bounded render/play probing with local extracted-cast layout resolution for sidecar paths such as `CastName/CastName.cst`.
- Native browser-bridge fixture coverage is verified with `libreshockwave_wasm_bridge_probe`. The no-argument default-root bridge probe maps absent `/var/html` to `/var/www/html` and currently scans 6,091 candidates with 6,031 loaded, 60 skipped, 0 failed, 430 pending host fetches delivered from local sidecars, 42,827,200 RGBA render bytes, 12,897 collected sprites, 1,257,183 nonblack pixels, and 0 script errors. The no-argument default-root 20-frame bridge scan currently passes with 6,031 loaded, 60 skipped, 0 failed, 438 pending host fetches delivered from local sidecars, 471 scanned frames, 436 frames with sprites, 436 nonblack frames, 23 files with nonblack scanned frames, and 0 script errors.
- Native browser-bridge probing of `/opt/git/v1_assets/projectorrays_lingo` through the exported C ABI currently passes with 64 files loaded, 0 skipped, 0 failed, 218 pending external-cast fetches delivered from local extracted sidecar layouts, 32,820,800 RGBA render bytes, 12,890 collected sprites, 962,156 nonblack pixels, and 0 script errors.
- Bounded lifecycle browser-bridge probing over `/var/www/html/dcr0910` currently passes with 64 files loaded, 0 skipped, 0 failed, 229 pending host fetches delivered from local sidecars, 1 queued `gotoNetMovie`, 35 stepped active-stage frames with sprites/nonblack pixels, and 0 script errors.
- The Habbo loader at `http://localhost/dcr0910/loader.dcr` was used as a completed browser proof: native browser-bridge probing reached the loader movie scripts (`startLoading`, `checkload`, and `loadComplete`), delivered all 10 listed sidecar asset fetches, and queued `gotoNetMovie("habbo_entry.dcr")` without script errors after resolving movie-script metadata and Director-version local slot scaling.
- Emscripten-built C++ WASM browser verification completed the same Habbo proof in the generated C++ web player under headless Chromium with cross-origin isolation, zlib-enabled XFIR/Afterburner decompression, C++ exception catching, captured WASM memory, and SharedArrayBuffer frame transport. The browser followed `gotoNetMovie("habbo_entry.dcr")`, reached the 151-frame Habbo entry movie with 99 visible sprites, reported no page/runtime errors, and rendered the Habbo Hotel login splash shown in the provided reference screenshot, including the yellow Habbo Hotel header, orange hotel building, and top-right "First time here?" and login panels.
- Browser movie-load/render verification is now kept generic with `node cpp/tools/browser_fixture_check.js --movie <movie-path>` after building `libreshockwave_cpp_wasm`. The tool serves the generated C++ WASM player plus a configurable fixture root, loads requested movies, waits for rendered nonblank frames, and runs generic worker WebSocket and C++ SMUS bridge diagnostics against local echo servers. It supports single-movie checks, comma-separated `--movies`, newline `--movie-list` files, and fixture discovery with `--discover-root`, while reusing one browser/server setup for batch runs. For extracted fixture roots, the local test server resolves Director external-cast requests to sidecar layouts such as `CastName/CastName.cst`, matching the native probe-side handling of v1 assets.
- Additional parity assets are already available under `/opt/git/v1_assets/`; these v1 assets are known to work correctly in the existing LibreShockwave Java implementation and should be used as a C++ runtime/player parity reference.
- Emscripten-built C++ WASM browser batch verification passes across all 21 `.dcr` movies under `/var/www/html/dcr0910` using `--discover-root dcr0910`. The batch reaches 210 total browser frames, renders 988,440 total nonblack pixels, reports no page/runtime errors, and passes generic WebSocket and C++ SMUS bridge diagnostics for every movie.
- Emscripten-built C++ WASM browser verification now also passes across all 21 `.dir` movies under `/opt/git/v1_assets/projectorrays_lingo` using the generic browser checker with `/opt/git/v1_assets/projectorrays_lingo` as the fixture root. Each movie reaches 10 browser frames, renders nonblank 720x540 canvas output, reports no page/runtime errors, and passes the generic WebSocket and C++ SMUS bridge diagnostics. Representative runs include `/gf_cafe/gf_cafe.dir` with 707 collected sprites and 8,445 nonblack pixels, `/habbo_entry/habbo_entry.dir` with 71 collected sprites and 296,722 nonblack pixels, and `/loader/loader.dir` with 3 collected sprites and 1,658 nonblack pixels.
- Browser WebSocket -> JS bridge networking is verified with the generated C++ WASM web player under headless Chromium against local WebSocket echo/error servers. The `runMusWebSocketSelfTest` diagnostic confirms direct worker-side connection, binary send, receive, close/disconnect, and connection-error delivery through the same worker-side Multiuser socket queues used by normal playback. The `runCxxSmusBridgeSelfTest` diagnostic confirms C++ queued SMUS connect requests are pumped into browser WebSockets, C++ emits the 80-byte SMUS logon frame, C++ packs and sends multiple SMUS chat frames over the same WebSocket, echoed SMUS bytes are delivered back into C++ and decoded as ordered sender/subject/content messages, the bridge remains connected between messages, and C++ disconnect/error delivery remains observable. The worker keeps bridge diagnostics exclusive from playback tick delivery so active movie ticks cannot drain diagnostic Multiuser events before the self-tests observe them.
- `/var/html` is not present in the current environment; the native C++ probe tools use `/var/www/html` as the local mapping for their default `/var/html` fixture root and that mapped full-corpus path is verified above.
- Before saving C++ port slices, run the CMake build, CTest, `git diff --check`, and relevant fixture probes.

## Remaining Major Work

- Higher-level media integration beyond linked member chunk discovery.
- Remaining sound, renderer-side PFR anti-alias fidelity, platform text shaping, and text/score/script chunk decoder edge cases.
- Remaining Lingo decompiler, VM runtime value, dispatcher, and builtin parity gaps.
- Remaining player core, rendering pipeline, input, networking, audio, cast management, and debugging parity gaps.
- Emscripten-built C++ WASM browser parity against broader historical web-player scenarios.
- Port parity tests against current fixtures and integration scenarios.
