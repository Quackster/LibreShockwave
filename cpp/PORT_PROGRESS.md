# LibreShockwave C++ Port Progress

Objective: port LibreShockwave's non-editor runtime/player stack to C++ while keeping the C++ tree buildable and tested after each slice. The editor is out of scope for the current port objective.

## Current Status

Started. The Java/Gradle project remains the authoritative implementation for behavior that has not yet reached C++ parity. A first-party CMake/C++ port exists under `cpp/` and is being expanded from reusable SDK foundations upward. Existing editor scaffolding is not being advanced as part of the current port objective.

This file tracks the current state and remaining work only. Detailed chronological changes live in git.

## Current C++ Coverage

- Build: root CMake integration, `LibreShockwave::libreshockwave`, CTest coverage, optional zlib support, and optional GTK4 editor target.
- Core file parsing: endian-aware binary IO, FourCC/MoaID helpers, Director chunk metadata, RIFX/XFIR loading including little-endian XFIR memory maps, D3 RIFF loading, Afterburner map/decompression support, lazy chunk reparse, and chunk categorization.
- Director chunks and metadata: config, palette, bitmap, sound/media, text, font maps, cast lists, cast members, key tables, score, large sparse score frame data, frame labels, script names, script contexts, and script bytecode.
- Cast/media SDK: cast-member wrappers, cast-library lookup, palette resolution, bitmap decoding, bitmap drawing/color/ink helpers, sound conversion, W3D entry/node/texture/resource-ref parsing plus named mesh/material/resource lookups, mesh vertex/face decoding, and material color/texture decoding, generated font decoding, bitmap font/PFR/TTF helpers, and utility formatting/string helpers.
- Lingo and VM foundations: datum model/formatting, opcode metadata, decompiler nodes/translation, value parsing, VM scope/execution context/runtime/safepoints/tracing/alert hooks/deferred dispatch, opcode registry handlers, builtin registry, and major builtins for math, strings, lists, output, control flow, constructors, types, casts, images, networking, sound including handled `beep`, top-level `_sound`, movie-backed `soundEnabled`, and `_sound` `soundLevel`/`soundKeepDevice`/`soundMixMedia`, Xtras, and timeouts.
- Runtime/player foundations: player facade, builtin context, VM event dispatch, sprite and movie properties including mutable sound flags and runtime capability checks (`multiSound`, `netPresent`, `safePlayer`), score navigation, input state/handler, hit testing, cursor manager, sprite registry/state, behavior instances, event dispatching, frame context, timeout manager, sound manager with queued elapsed-time tracking, sound-enabled playback gating, and global sound-level volume scaling, net manager, queued browser-facing net/audio/JPEG/Multiuser bridges, default player text renderer wiring, and player-owned Xtra manager wiring.
- Rendering pipeline: stage renderer, sprite baker, render pipeline context/runner, software frame renderer, bitmap cache/ink processor, text renderer interface, simple text renderer, and verbose render-probe diagnostics for unbaked sprite/member types.
- Dynamic/runtime member support: runtime bitmap/text/palette/script payloads, dynamic member creation/reuse/erase, runtime member media copying, imported bitmap media, runtime registration points, editable field state/overlay helpers, and dynamic bitmap/text rendering through the player pipeline.
- Debug/editor foundations: debugger data/control models, breakpoint/watch/expression helpers, lifecycle diagnostics, GTK-neutral editor models, optional GTK shell scaffolding, menu/action/dialog/open-file/workbench/start-screen metadata, dock/floating pane state, GTK4 right-click pane menus, live floating-pane dragging, drag-snap hooks, fixed-position floating pane rendering, native palette-backed color palette panel state, and GTK4 action/menu/open-file scaffolding.
- Browser/WASM foundations: C++ WASM player wrapper, runtime bridge, prefixed native-testable C ABI export facade, browser-side export adapter for the worker's TeaVM-style method names, C++-owned browser bootstrap page/player/worker assets, self-checking adapter/header/export-list/browser-asset contract tests, and conditional Emscripten CMake packaging, covering movie loading, stale async load suppression, nonblank score-sprite frame rendering into the JS-facing RGBA buffer, browser worker-to-canvas RGBA blitting, optional SharedArrayBuffer/Atomics frame transport with transferable fallback, cursor/caret/selection overlay compositing, clipboard text selection commands, debug log flushing, trace controls, call-stack/sprite/text/bootstrap/runtime diagnostics, playback/input controls, exported script deadline wiring, navigation polling, external params, POST/fallback fetches, cross-origin main-thread fetch relay, browser image import decode, audio command forwarding with stale callback protection, Multiuser WebSocket bridging, embedded JPEG decode queues, host result delivery, and last-error retrieval.
- Fixture validation: native `libreshockwave_probe` scans Director `.cct`, `.cst`, `.dcr`, `.dir`, and `.dxr` files through C++ load, editor member-scan, and score-grid paths while skipping extension-matched non-Director placeholder files, reporting processed-file progress, and optionally reporting the active file during long scans. Native `libreshockwave_render_probe` loads the same fixture set through the player renderer, preloads local external casts through the C++ player/net path, reports processed-file progress, produces frame-buffer statistics, can scan score frames for nonblank output, and can optionally run bounded `play()`/tick lifecycle dispatch before rendering. Native `libreshockwave_wasm_bridge_probe` drives the browser-facing C++ WASM C ABI exports against the same fixture set, satisfies pending host fetches from local sidecar files, renders through the exported RGBA buffer API, captures bounded playback errors, and scans active-stage frames by stepping the exported player.

## Verification

- C++ coverage is verified through the `libreshockwave_cpp_tests` CTest executable.
- Browser bridge coverage includes native C ABI export tests that load a synthetic Director movie, start playback, render a shaped score sprite, verify exact RGBA bytes from the exported render buffer, verify exported script deadline controls, and keep the adapter's required exports synchronized with the C ABI header and Emscripten export list.
- C++ browser bootstrap coverage includes resource contract tests that verify the C++ web page/player/worker assets instantiate `libreshockwave-cpp-wasm.js`, use the exported C ABI for movie loading, stale async load suppression, local/POST/fallback fetch delivery, cross-origin main-thread fetch relay, browser image import decode, embedded JPEG decode, audio command forwarding, Multiuser WebSocket events, navigation polling, frame rendering, optional SharedArrayBuffer/Atomics frame transport, cursor/caret/selection overlays, clipboard text selection commands, debug log flushing, trace controls, call-stack/sprite/text/bootstrap/runtime diagnostics, script deadlines, input forwarding, and canvas blitting, and are copied by the Emscripten CMake target.
- Fixture coverage is verified with `libreshockwave_probe` against local Director fixture roots. `/var/www/html/dcr0910` currently passes with 64 files loaded, 0 skipped, and 0 failed. Full `/var/www/html` fixture scanning currently passes with 6,031 files loaded, 60 skipped, and 0 failed.
- Player render fixture coverage is verified with `libreshockwave_render_probe`. Full `/var/www/html` static render probing currently passes with 6,031 files rendered, 60 skipped, 0 failed, 430 external cast entries fetched/loaded from local sidecars, 80 collected sprites, 1 baked sprite, and 721 non-background pixels. Full `/var/www/html` 20-frame score-frame scan coverage currently passes with 612 scanned frames, 576 frames with sprites, 564 frames with baked sprites, 435 nonblank frames, 23 files with nonblank scanned frames, and 0 script errors; verbose scan output currently has no non-empty genuine unbaked render-target fields after separating expected missing/script score references into `nonRenderable*` diagnostics. Bounded lifecycle render probing over `/var/www/html/dcr0910` currently passes with 64 files rendered, 0 skipped, 0 failed, 218 external cast entries fetched/loaded from local sidecars, 12,901 collected sprites, 12,893 baked sprites, 159,930 non-background pixels, and 0 script errors.
- Native browser-bridge fixture coverage is verified with `libreshockwave_wasm_bridge_probe`. Full `/var/www/html` probing through the exported C ABI currently passes with 6,031 files loaded, 60 skipped, 0 failed, 430 pending host fetches delivered from local sidecars, 42,827,200 RGBA render bytes, 66 stepped active-stage frames scanned, 31 frames with sprites, 31 frames with nonblack pixels, and 2 files with nonblack stepped frames.
- Bounded lifecycle browser-bridge probing over `/var/www/html/dcr0910` currently passes with 64 files loaded, 0 skipped, 0 failed, 229 pending host fetches delivered from local sidecars, 1 queued `gotoNetMovie`, 35 stepped active-stage frames with sprites/nonblack pixels, and 0 script errors.
- Native browser-bridge probing of `http://localhost/dcr0910/loader.dcr` now reaches the loader movie scripts (`startLoading`, `checkload`, and `loadComplete`), delivers all 10 listed sidecar asset fetches, and queues `gotoNetMovie("habbo_entry.dcr")` without script errors after resolving movie-script metadata and Director-version local slot scaling.
- Emscripten-built C++ WASM browser verification now loads `http://localhost/dcr0910/loader.dcr` in the generated C++ web player under headless Chromium with cross-origin isolation, zlib-enabled XFIR/Afterburner decompression, C++ exception catching, captured WASM memory, and SharedArrayBuffer frame transport. The browser follows `gotoNetMovie("habbo_entry.dcr")`, reaches the 151-frame Habbo entry movie at frame 33 with 99 visible sprites, reports no page/runtime errors, and renders the expected Habbo Hotel login splash with the yellow Habbo Hotel header, orange hotel building, and top-right "First time here?" and login panels visible.
- Expected browser parity target: `http://localhost/dcr0910/loader.dcr` should continue to render the Habbo Hotel login splash shown in the provided reference screenshot.
- Additional parity assets are already available under `/opt/git/v1_assets/`; these v1 assets are known to work correctly in the existing LibreShockwave Java implementation and should be used as a C++ runtime/player parity reference.
- Browser WebSocket -> JS bridge networking is verified with the generated C++ WASM web player under headless Chromium against local WebSocket echo/error servers. The `runMusWebSocketSelfTest` diagnostic confirms direct worker-side connection, binary send, receive, close/disconnect, and connection-error delivery through the same worker-side Multiuser socket queues used by normal playback. The `runCxxSmusBridgeSelfTest` diagnostic is run after loading `/dcr0910/loader.dcr` and confirms C++ queued SMUS connect requests are pumped into browser WebSockets, C++ emits the 80-byte SMUS logon frame, C++ packs and sends a SMUS chat frame, echoed SMUS bytes are delivered back into C++ and decoded as sender/subject/content, and C++ disconnect/error delivery remains observable. The worker keeps bridge diagnostics exclusive from playback tick delivery so active movie ticks cannot drain diagnostic Multiuser events before the self-tests observe them.
- Real fixture-scripted browser Multiuser coverage is verified after loading `http://localhost/dcr0910/loader.dcr` in the generated C++ WASM web player. The `runFixtureMultiuserScriptSelfTest` diagnostic confirms the loaded external `FuseScript` cast exposes `Logon`, `sendFuseMsg`, and `DefaultMessageHandler`, the real `Logon` Lingo handler queues a `ConnectToNetServer` request through the browser WebSocket bridge, the connected callback sets `gConnectionOk`, and the real `sendFuseMsg("STATUSOK")` path queues and sends the expected plaintext FUSE payload `8   STATUSOK`.
- `/var/html` is not present in the current environment; available local web fixtures are under `/var/www/html`.
- Before saving C++ port slices, run the CMake build, CTest, `git diff --check`, and relevant fixture probes.

## Remaining Major Work

- Higher-level media integration.
- Remaining sound, renderer-side PFR anti-alias fidelity, platform text shaping, and text/score/script chunk decoder edge cases.
- Remaining W3D shader/material variants, animation data, scene graph behavior, and rendering integration.
- Remaining Lingo decompiler, VM runtime value, dispatcher, and builtin parity gaps.
- Remaining player core, rendering pipeline, input, networking, audio, cast management, and debugging parity gaps.
- Emscripten-built C++ WASM browser parity against the existing TeaVM web-player scenarios, including additional real fixture-scripted SMUS/Multiuser gameplay/server parity beyond the verified bridge and FUSE `Logon`/`sendFuseMsg` coverage.
- Confirm end-to-end WebSocket -> JS bridge networking and SMUS behavior against real playback/server scenarios, beyond the current diagnostic bridge coverage.
- Full `/var/html` `.cct`/`.dcr` read/handling verification once that requested fixture root exists or is mapped.
- Port parity tests against current fixtures and integration scenarios.
