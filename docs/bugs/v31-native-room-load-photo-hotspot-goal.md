# Goal: V31 loader — native room entry flow and wall-photo hotspot transition

## Objective
- Main goal: after entering the v31 room, click the photo furni item on the wall, open the photo, and render the opened photo state correctly.
- The target rendered state is `/opt/git/v31_room_load/v31_native_rooms_entered_clicked_photo.png`.
- Pixel-perfect means the entire actual `959x540` reference frame must match exactly, not just the opened photo bitmap or photo window. The dropdowns, menus, bottom bar, right action panel, room chrome, and every other visible UI element in the final screenshot are part of the target and must have `exactMismatch=0`.
- Fix the bug from root cause only.
- No bandaid fixes.
- No client shims or compatibility hacks.
- Do not introduce single-purpose fallbacks for only this hotspot.
- Do not patch `Player.java` with per-frame room-visualizer repair logic.
- Do not force sprite/member/offset state from outside the normal runtime object, property, sprite, or event paths.
- Any fix must explain why the original runtime path produced the wrong state and correct that path directly.
- Do not use pure Java test paths for v31 hotspot validation (WASM only).

## Current problem to solve
- V31 native room entry reaches `v31_native_rooms_entered.png`, and the wall-photo click can visually match `v31_native_rooms_entered_clicked_photo.png`, but the runtime still reports `[ScriptError] Error in renderWallRandomProps`.
- The primary failure to fix is now the runtime script error during room render, because a visual screenshot pass with a script-error redirect is not acceptable.
- The opened-photo hotspot render remains the main goal: click the photo furni item on the wall, open the photo, and render the opened photo state correctly.
- The previous `Player.java` repair approach is explicitly unacceptable because it repairs observed state after the fact instead of correcting the runtime path that created the bad state.
- `RuntimeObjectBindingHooks` is also explicitly unacceptable because it is a side-channel binding hook rather than the underlying VM/runtime semantic fix.

## Investigation notes
- Root cause found in the VM data model, not the room client flow: Director prop-list keys are full values, but several LibreShockwave paths collapsed keys to `toKeyName()` strings/symbols.
- V31 `renderWallRandomProps` stores random wall prop images under point-like keys such as `point(141, 36)`. When those keys lose datum identity, `getPropAt()` can return the wrong key type and the script cannot reliably read `.locH` and `.locV`.
- A second VM semantic gap appears in the same handler: random wall prop values can include `(image null)`. Director-compatible empty image references must expose zero dimensions/empty rect semantics, not throw when Lingo reads `.width`, `.height`, `.rect`, or duplicates the value.
- Required fix direction: preserve raw prop-list key datums through bytecode prop-list construction, parsed prop-list values, `addProp`/`setaProp`, prop-list method dispatch, `getPropAt`, `getOne`, `duplicate`, and nested property writes.
- Required fix direction: make null `ImageRef` behave as an empty image for core image property reads instead of raising Java null-pointer errors inside script execution.
- Current code evidence: `Player.java` must not contain `repairRoomVisualizerSpriteWrappers`, and `RuntimeObjectBindingHooks` must not exist or be used.
- Focused WASM evidence before the final image-null fix: `./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest --no-daemon` passed the visual photo-open reference, but still emitted `[ScriptError] Error in renderWallRandomProps`.
- Focused WASM evidence after the prop-list key and null-image fixes: `./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-after-image-null-fix --no-daemon` passed the visual photo-open reference and did not emit `[ScriptError] Error in renderWallRandomProps`.
- Diagnostic trace evidence from that focused pass showed `renderWallRandomProps` receiving point-keyed prop-list entries as datum keys, for example `point(141, 36): (image 10x9)`, instead of only stringified keys.
- Current no-trace focused WASM evidence: `./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-after-trace-removal --no-daemon` passed after removing the temporary `renderWallRandomProps` trace hook and still did not emit the previous script error.
- Latest rejected-bandaid audit: searching production code and the v31 WASM test for `repairRoomVisualizer`, `RuntimeObjectBindingHooks`, and `addTraceHandler('renderWallRandomProps')` found no production/test hook occurrences; only this goal document mentions `RuntimeObjectBindingHooks` as an explicitly rejected approach.
- Correction to regression-gate scope: the v1/v14 non-regression requirement is not the host-backed navigator/private-room/public-room flow and must not require TCP/MUS/proxy connectivity, posts, login, room entry, or any external server connection.
- The intended v1/v14 regression gate is the existing offline movie-load visual comparison path that loads the v31/v14/v1 movies and compares their initial native renders against `/opt/git/v14_v31_compare/`.
- Harness update: `player-wasm/src/test/js/wasm-v31-room-photo-hotspot-native-test.js` now runs the existing `wasm-native-visual-regression-test.js` for `LS_NATIVE_VISUAL_CASES=v1,v14` before executing the v31 room-photo flow, so the focused hotspot test also proves the v1/v14 offline movie-load render guard.
- Current combined WASM evidence: `./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-with-legacy-visuals --no-daemon` passed. The task first passed the embedded v1/v14 offline native visual checks using `/opt/git/v14_v31_compare/`, then passed the v31 rooms -> Go -> wall-photo click flow and matched `/opt/git/v31_room_load/v31_native_rooms_entered_clicked_photo.png`.
- Test naming update: the room-entry portion is saved as `roomEnterTestV31`, and the photo-opening test is `roomEnterTestV31ClickPhoto`.
- Required active hotspot test: `roomEnterTestV31ClickPhoto`.
- `roomEnterTestV31` must use `sw9=forward.type=2;forward.id=1` to go straight into the room; it must not click `Go`.
- The expected screen before clicking the photo is `/opt/git/v31_room_load/v31_native_rooms_entered.png`, not `/opt/git/v31_room_load/v31_native.png`.
- `roomEnterTestV31ClickPhoto` must first reach the entered-room reference via `roomEnterTestV31`, then click the photo furni item on the wall, and finally pixel-diff the post-click render against `/opt/git/v31_room_load/v31_native_rooms_entered_clicked_photo.png`.
- The click-photo test must save the actual opened-photo render as `roomEnterTestV31ClickPhoto_opened_photo.png` and a stable copy as `v31_photo_opened.png` in the test output directory.
- The room-enter-only test must live in `player-wasm/src/test/js/wasm-v31-room-enter-native-test.js`.
- The click-photo test must live in `player-wasm/src/test/js/wasm-v31-room-photo-hotspot-native-test.js`.
- The active test right now is the click-photo test only: `roomEnterTestV31ClickPhoto` via `./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest`.
- Current split-test evidence: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/roomEnterTestV31ClickPhoto --no-daemon` passed.
- Current split-test behavior: `roomEnterTestV31` waited directly for `/opt/git/v31_room_load/v31_native_rooms_entered.png` without clicking `Rooms` or `Go`, then `roomEnterTestV31ClickPhoto` clicked the wall photo and matched `/opt/git/v31_room_load/v31_native_rooms_entered_clicked_photo.png`.
- Current split-test output: `/opt/git/LibreShockwave/build/roomEnterTestV31ClickPhoto/roomEnterTestV31ClickPhoto_opened_photo.png`.
- Stable saved output: `/opt/git/LibreShockwave/build/roomEnterTestV31ClickPhoto/v31_photo_opened.png`.
- Current post-click diff evidence: mean delta `14.465201019567152`, bad fraction `0.09270266095083614`, match fraction `0.7991909010157185`, max delta `255`, which passes the configured v31 room-photo thresholds.
- Exact-match audit correction: threshold passing was too weak for the current goal. A fresh focused run at `/opt/git/LibreShockwave/build/v31-photo-exact-audit` produced `roomEnterTestV31ClickPhoto_opened_photo.png` with the same `959x540` dimensions as the target, but `cmp` failed and ImageMagick reported `211525` differing pixels (`40.846%`). The saved frame had not opened the photo; it was still the closed-photo room state. This means the previous loose threshold could falsely pass a non-opened target.
- Harness hardening in progress: `player-wasm/src/test/js/wasm-v31-room-photo-hotspot-native-test.js` now computes `exactMismatch`/`exactMatchFraction`, requires `exactMismatch === 0` for the photo-click stage, saves last actual/diff frames for failed exact attempts, makes exact poll count configurable via `LS_V31_ROOM_PHOTO_EXACT_POLLS`, and makes max photo click attempts configurable via `LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS`.
- Harness candidate correction in progress: the manual photo click points were previously broad `160x160` regions prepended ahead of detected sprites. They now target the actual wall photo region around `(415,100)` and diagnostic candidate sorting prioritizes sprites whose member/cast/dynamic names include `photo`.
- Current exact-click evidence before MUS-buffer fix: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_EXACT_POLLS=5 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-exact-attempt0 --no-daemon` failed as expected under exact matching. The first click opened the photo window, but the image area showed `photo_invalid` instead of the real photo. Metrics for `roomEnterTestV31ClickPhoto_photo_click_attempt_0_last.png`: `exactMismatch=186383`, `exactMatchFraction=0.6400899857104236`, `meanDelta=11.33259632591279`, `badFraction=0.06429343838102962`, `spriteCount=70`.
- New root-cause lead: the photo window is not a hotspot-routing problem after the first corrected click. `Photo Component Class.binaryDataReceived` assigns `pPhotoMember.media = tdata[#image]`, validates `me.countCS(pPhotoMember.image) = tdata[#cs]`, and falls back to `member(getmemnum("photo_invalid")).media` when checksum fails. The current exact-click frame visually matches that invalid-picture fallback.
- MUS transport finding: in the WASM worker, inbound Multiuser WebSocket messages were copied into the shared string buffer with `var len = Math.min(msgBytes.length, 4096)`, even though `WasmEntry.stringBuffer` is `65536` bytes. Photo binary retrieval uses Fuse binary manager over MUS (`retrieveBinaryData` -> `GETBINDATA` -> `binaryDataReceived`), so truncating inbound MUS chunks at 4096 bytes can corrupt valid photo binary data and trigger the checksum failure/invalid image path.
- MUS transport fix attempt: `WasmEntry` now exports `getStringBufferCapacity()`, and `shockwave-worker.js` uses that capacity instead of a fixed 4096 byte limit when delivering inbound MUS messages.
- MUS transport validation result: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_EXACT_POLLS=10 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-after-mus-buffer --no-daemon` still failed exact matching. The first click again opened the photo window but rendered `photo_invalid` with the same metrics as before (`exactMismatch=186383`, `meanDelta=11.33259632591279`, `badFraction=0.06429343838102962`, `spriteCount=70`). This means the fixed 4096 delivery cap was either not the active corrupting path for this payload, or another MUS/binary deserialization/media assignment issue remains downstream.
- MUS diagnostic log correction: the harness `onDebugLog` filter previously dropped `[MUS]` logs. It now preserves `[MUS]` and `[MultiuserXtra]` diagnostics, and the failure path always writes `browser_console.log`.
- Current MUS log finding: in `/opt/git/LibreShockwave/build/v31-photo-mus-debug-logs`, the room socket uses MUS instance `1` at `verysecret.classichabbo.com:30100`. After clicking the wall photo, the binary manager opens MUS instance `2` at `verysecret.classichabbo.com:39101`, which matches the required loader. The exact check sampled the invalid-photo frame immediately after instance `2` opened, before logs showed binary-server `HELLO`/`LOGIN`/`GETBINDATA` response traffic. Next diagnostic step is a longer exact wait with MUS logs enabled to distinguish “binary data arrives but decodes/checksums wrong” from “the harness captures before binary data arrives.”
- Longer MUS wait result: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_EXACT_POLLS=120 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-mus-long-wait --no-daemon` failed. The photo window stayed in the same placeholder/invalid state for 100+ logged exact polls (`exactMismatch=186383`). Logs show instance `2` opens on `39101`, but no subsequent instance-2 `send`, `message`, or `deliver` events are logged during the wait; only room instance `1` continues traffic. The current active failure is therefore before photo image decode/checksum: the binary-manager MUS connection is opened but does not progress through its handshake/login/retrieve-data path.
- Native Lingo path audit: `ParentScript 52 - Multiuser Instance Class.ls` sets `pConnectionOk = 1` whenever its `xtraMsgHandler` fires, forwards string content as `subject + RETURN + content`, and sends only raw message content via `pXtra.sendNetMessage("*", tPartOne, tPartTwo)`. `ParentScript 42 - Binary Manager Class.ls` correctly registers `HELLO` to `#helloReply`, waits for both `connectionReady()` and `pHandshakeFinished`, then sends `LOGIN` and later `GETBINDATA`. Therefore the stuck state is not currently a photo decode/checksum problem and not an obvious Lingo listener-registration miss; it is the absence of the binary-server `HELLO` payload on MUS instance `2`.
- Desktop/WASM transport comparison: `SocketMultiuserBridge` also writes only `content.toStr()` as raw bytes and reads raw available bytes back into the message content, so the WASM worker's raw-content send model matches the desktop bridge at this layer. The active question is why the WebSocket connection to `verysecret.classichabbo.com:39101` opens but produces no inbound raw payload during the exact wait.
- Direct WebSocket probe: `ws://verysecret.classichabbo.com:30100` sends `@@\1` immediately after open, but `ws://verysecret.classichabbo.com:39101` opens and sends no data for an 8 second standalone wait. Sending the room acknowledgement `@@CCNH` to `39101` closes the target; sending plaintext `HELLO`, `LOGIN 1 machine`, or `GETBINDATA 1 x` does not produce useful data. This supports the in-harness observation that the binary port does not emit the expected `HELLO` without additional correct client-side behavior.
- Diagnostic synthetic-handshake result: temporarily injecting inbound `HELLO 0` for the `39101` WebSocket allowed the Binary Manager to leave its handshake wait, but the bridge then logged instance-2 sends as only the arguments (`"1"` and `"5 <machine-id>"`) and the binary socket closed. That exposed a concrete transport serialization bug: `ParentScript 52` uses `sendNetMessage("*", command, args)`, but the bridge was transmitting only `content`, dropping command subjects such as `LOGIN` and later `GETBINDATA`.
- Transport serialization fix in progress: `WasmMultiuserBridge.PendingRequest.wireContent()` now serializes non-empty/non-`0` subjects as `subject + " " + content`, while preserving raw-content sends for subject `0`/empty. `WasmEntry.getMusPendingSendData()` uses that wire content, and `SocketMultiuserBridge` has the same serialization rule. This still needs validation; the temporary synthetic `HELLO 0` remains diagnostic-only until the correct binary-server handshake behavior is identified.
- Venus server context: the active MUS backend is in `../Venus`, not an unknown external service. Venus documents `server.mus.port=38101` and, when `server.websocket.enabled=true`, `server.websocket.mus.port=38201` by default; `39101` is not the Venus default unless the running process overrides it. The v31 loader currently still says `connection.mus.port=39101`, so the next check is to confirm the actual running Venus WebSocket MUS port before treating `39101` behavior as the server truth.
- Venus camera SMUS handler: `../Venus/venus-server/src/main/java/org/oldskooler/venus/game/camera/CameraSmusMessageHandler.java` handles subjects `LOGIN`, `PHOTOTXT`, `BINDATA`, and `GETBINDATA`. `GETBINDATA` parses `<storedId> <authToken>`, looks up `CameraPhotoDao.findByStoredIdAndAuthToken`, sends `BINDATA_AUTHKEYERROR` if missing, and otherwise replies with subject `BINARYDATA` containing a property list with `image`, `time`, and `cs`. This is exactly the expected backend for `hh_photo`; the remaining client-side problem is connecting with Venus's real SMUS-over-WebSocket protocol/port rather than a synthetic `HELLO`.
- Venus SMUS protocol audit: `../Venus/venus-server/src/main/java/org/oldskooler/venus/smus/SmusWebSocketServer.java` accepts only binary WebSocket frames and passes them to `SmusProtocolSession.handleWireBytes`. `SmusProtocolSession` requires a complete SMUS frame (`0x72 0x00` plus 32-bit body length), and the first message must be subject `Logon` with login content encrypted by key `IPAddress resolution`. Venus tests show the intended client logon payload is a Lingo list of `["*", "*", "1,0,2,142"]`; after that Venus sends SMUS subjects `Logon` and `HELLO`. Therefore the LibreShockwave worker's raw text WebSocket bridge is incompatible with Venus MUS for the photo path until it frames/unframes real SMUS messages.
- Diagnostic cleanup: the temporary worker-side synthetic `HELLO 0` was removed after the Venus protocol audit. It proved that command subjects were previously being dropped, but it is not a valid path for Venus because the server requires a real encrypted SMUS `Logon` frame before it sends the legitimate `HELLO`.
- LibreShockwave SMUS implementation step: `MultiuserXtra.connectToNetServer` now preserves the sixth mode argument and passes it to `MultiuserNetBridge`. The WASM bridge keeps the existing raw byte stream behavior for mode `1` room traffic, but mode `0` instances now use SMUS framing: after WebSocket open they send Venus's encrypted `Logon` frame, outgoing `sendNetMessage` calls become SMUS frames with Lingo-encoded content, and inbound binary WebSocket frames are decoded back into normal Multiuser Xtra messages. A narrow `Datum.Media` value was added so Venus `BINARYDATA` property-list media can reach `pPhotoMember.media`; bitmap members currently accept that media when it is in the existing `LSWI` RGBA import format. `:player-wasm:compileJava` passes after these changes. The focused photo test still needs to verify whether the stored Venus photo bytes are `LSWI` or a different Director media format.
- TeaVM compatibility note: directly depending on `com.github.Quackster:jSMUS` compiled under javac but failed during `:player-wasm:generateWasm` because TeaVM's class library rejected jSMUS bytecode calls to `ByteArrayOutputStream.writeBytes(byte[])`. The bridge was changed to use a local minimal SMUS/Lingo codec for WASM and a precomputed Venus-compatible 80-byte encrypted `Logon` frame generated from Venus's jSMUS code. This avoids vendoring the full Blowfish table and keeps the unsupported jar out of the TeaVM graph.
- SMUS verification result: the focused photo test now reaches the real Venus photo path. Instance `2` on `39101` sends the encrypted SMUS `Logon`, receives Venus `Logon` and `HELLO`, sends `LOGIN` and `GETBINDATA`, and receives a `BINARYDATA` prop list with media. Filtering the protocol-level server `Logon` at the bridge layer removed the earlier minor "Unknown" script error. The remaining non-exact frame is after `BINARYDATA` delivery, so the active failure moved from transport/handshake to decoding the returned media into the photo bitmap member.
- Venus media finding: the returned `#image` value is not `LSWI`; it is a 15572-byte Director bitmap media container. The diagnostic blob has `BitmapInfo` at offset `28`, a little-endian `DTIB`/`BITD` marker at offset `60`, BITD length `15504`, and describes a 161x117 8-bit bitmap with pitch `162`. Existing `BitmapDecoder.decode(..., Palette.SYSTEM_MAC_PALETTE, true, 1200, 162)` successfully decoded the payload locally, and the sampled photo checksum was `78151`. Implementation step in progress: route `Datum.Media` bitmap assignment through a reusable media decoder that tries `LSWI` first, then parses this Director `BITD` container and assigns the decoded bitmap to `member.media`.
- Follow-on focused run result: after adding the Director `BITD` media decoder, the test still rendered the exact same invalid-photo fallback (`exactMismatch=186383`) even though `BINARYDATA` arrived and decoded. The decompiled photo script shows why: `Photo Component Class.countCS` uses `tImg.getPixel(...).paletteIndex`, while `ImageMethodDispatcher.getPixel` returned only RGB `color(r,g,b)` values. The next implementation step preserves decoded bitmap palette-index metadata through `getPixel` by returning `paletteIndex(n)` for indexed bitmaps and adds a `paletteIndex` property accessor for those color values, so the original Director checksum expression can evaluate against raw 8-bit indices.
- Palette-index checksum diagnostic result: after preserving indexed bitmap pixel metadata, the focused test still rendered the same invalid-photo frame (`exactMismatch=186383`). A temporary `BINARYDATA` diagnostic confirmed Venus sent `serverCs=78151` and LibreShockwave's local decoded image checksum also computed `localCs=78151` for the 161x117 8-bit payload. Therefore the remaining gap is no longer SMUS transport, media decode, or checksum semantics; the active investigation is whether the queued `BINARYDATA` reaches `Multiuser Instance Class.xtraMsgHandler` and `Photo Component Class.binaryDataReceived`, or whether the callback runs but the resulting member/render update is lost downstream.
- Photo member validation finding: `Photo Component Class.binaryDataReceived` immediately checks `(pPhotoMember.type <> #bitmap) or (pPhotoMember.scriptText <> EMPTY)` after assigning returned media. LibreShockwave exposed no generic `scriptText` cast-member property for runtime bitmap members, so `pPhotoMember.scriptText` evaluated as `VOID`, which is Director-incompatible and causes the script to erase the freshly decoded bitmap before checksum/render use. Implementation step: cast members now expose `scriptText` as `EMPTY` by default, matching the protected-player behavior expected by this bitmap validation path.
- Binary Manager callback boundary: a follow-up focused diagnostic showed `BINARYDATA` now reaches `Multiuser Instance Class.xtraMsgHandler` and dispatches into `Binary Manager Class.binaryDataReceived` (`ControlFlow` logs one `binaryDataReceived` call with a prop-list argument on the Binary Manager instance), but there is no second `call(#binaryDataReceived, tObject, tdata, tTask[#id])` into the Photo Component and no bitmap-member media assignment. The active gap is inside `Binary Manager Class.binaryDataReceived`, specifically either `tTask[#callback]` is `VOID`/wrong, or `getObject(tTask[#callback])` resolves to `0`/non-instance before the photo callback can run. Next diagnostic: log the queued task's `#callback` prop-list lookup and the Object Manager `pObjectList.getaProp(tID)` lookup result, because both depend on Director-compatible prop-list key semantics.
- Callback root cause found: the prop-list diagnostic showed `tTask[#callback] -> #photo_component` and `getObject(#photo_component) -> <script#327>`, so the callback and Object Manager lookup are valid. The branch stops at `if tObject.ilk = #instance` because `ilk(tObject)` was implemented for script instances, but object-property access `tObject.ilk` fell through to script-instance property lookup and returned `VOID`. Implementation step: script instances now expose `.ilk` as `#instance` through object-property and chained-property access, matching the Director property form used by Binary Manager.
- Post-callback progress: after the `.ilk` object-property fix, `BINARYDATA` reaches both Binary Manager and Photo Component. Diagnostics show `Photo Component Class.binaryDataReceived` runs on instance `327`, assigns the returned 15572-byte media payload to dynamic bitmap member `10110`, and decodes it as a 161x117 bitmap. The opened photo window now renders the real Venus photo rather than the `photo_invalid` fallback.
- Current exact-match failure moved to palette/color resolution. The focused run at `/opt/git/LibreShockwave/build/v31-photo-after-script-instance-ilk` still failed exact matching with `exactMismatch=186383`, `meanDelta=9.180790174962555`, `badFraction=0.046172710771250916`, and `exactMatchFraction=0.6400899857104236`. The visual state is structurally the opened-photo state, but the photo colors are wrong.
- Palette investigation result: decoding indexed Director media with the active palette slightly improved the frame (`meanDelta=9.164534816371479`, `badFraction=0.04584636774417796`) but did not fix exact matching. Resolving the media `BitmapInfo` palette reference through the dynamic member's cast library improved it further at `/opt/git/LibreShockwave/build/v31-photo-media-palette-ref` (`meanDelta=8.82109386063996`, `badFraction=0.03792917004595837`), but `exactMismatch` remained `186383`.
- Current color evidence from `/opt/git/LibreShockwave/build/v31-photo-media-palette-ref/roomEnterTestV31ClickPhoto_photo_click_attempt_0_last.png`: sampled photo pixels are `p{430,130}=srgba(56,44,22)`, `p{500,150}=srgba(40,32,16)`, and `p{520,225}=srgba(0,0,0)`, while the target has `srgba(160,75,38)`, `srgba(144,62,31)`, and `srgba(255,222,111)`. The next root-cause step is exact Director palette-ID resolution for the returned media, likely by resolving `BitmapInfo.paletteId` against cast/source-file palette IDs instead of only trying `paletteId + 1` as a member number or falling back to the active palette.
- Provider-level palette-ID resolver attempt: `CastLibProvider.resolvePaletteById` and `CastLibManager.resolvePaletteById` now reuse exact Director palette lookup in the hinted/default cast source file, then the main movie, then loaded casts before falling back. `:player-wasm:compileJava` passed. The focused run at `/opt/git/LibreShockwave/build/v31-photo-palette-id-resolver` still failed with the same post-palette-ref metrics (`exactMismatch=186383`, `meanDelta=8.82109386063996`, `badFraction=0.03792917004595837`), so the current resolver either selects the same wrong palette or the parsed media `BitmapInfo` palette fields are not the needed color source.
- Media palette diagnostic result: the returned Director media declares `paletteId=-3` and `paletteCastLib=-1`, i.e. the built-in `#grayscale` palette, not a missing custom cast palette. The decoded sample indices are `p8,12=199`, `p78,32=215`, and `p98,107=127`; LibreShockwave currently displays these through the inverted grayscale ramp as `0x383838`, `0x282828`, and `0x808080` before sprite ink processing.
- Post-click sprite diagnostic result: the opened photo image is dynamic bitmap member `10110` on sprite channel `144`, `161x117`, `ink=41` (`#darken`), `foreColor=0x681f10`, `backColor=0xffcc66`, `dynPal=Grayscale`, and `dynScript=true`. The target photo is the same content but much warmer/brighter. The active root-cause lead is now Director-compatible indexed-grayscale `#darken` sprite colorization/transparency, not SMUS transport, BINARYDATA dispatch, checksum, media assignment, or missing custom palette lookup.
- Indexed-sprite darken implementation step: `BitmapCache.resolveIndexedMatteColorRemap` now also permits `InkMode.DARKEN`, so dynamic indexed grayscale sprites can recolor from preserved palette indices through the sprite `foreColor`/`backColor` ramp after matte processing instead of multiplying the already-decoded inverted grayscale RGB. Added focused coverage in `ScriptModifiedBitmapTest.scriptModifiedIndexedDarkenUsesPaletteIndicesForSpriteColorRamp`; `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH ./gradlew :player-core:test --tests com.libreshockwave.player.ScriptModifiedBitmapTest --no-daemon` passed.
- Focused WASM result after the indexed `DARKEN` remap hook: `/opt/git/LibreShockwave/build/v31-photo-indexed-darken-remap` still failed with unchanged stable metrics (`exactMismatch=186383`, `meanDelta=8.82109386063996`, `badFraction=0.03792917004595837`) and unchanged samples (`p430,130=56,44,22`, `p500,150=40,32,16`, `p520,225=0,0,0`). A narrow `BitmapCache` probe in `/opt/git/LibreShockwave/build/v31-photo-darken-remap-cache-probe` showed the photo render path reaches the cache as `rawDepth=8`, `palette=Grayscale`, `hasFore=true`, `hasBack=true`, but `indices=null`. The active issue is now palette-index metadata being cleared after media decode and before sprite baking, likely by a follow-up image operation in the photo/timestamp path, not the initial `BITD` decoder or the sprite color ramp itself.
- Palette-index preservation implementation step: `ImageMethodDispatcher.copyPixels` no longer clears an indexed destination's entire palette-index plane when copying source pixels that cannot themselves preserve source indices. If the destination is an indexed image with a palette and existing indices, the copied rectangle is requantized through the destination palette and untouched pixels keep their original indices. This targets the photo member case where later text/timestamp drawing should not erase the base photo's BITD index metadata before sprite `#darken` rendering. Added `ScriptModifiedBitmapTest.copyPixelsOntoIndexedDestinationRefreshesOnlyAffectedPaletteIndices`; the focused `ScriptModifiedBitmapTest` class still passes.
- Additive indexed `DARKEN` correction: after palette-index preservation, the photo samples changed from the old multiply-only values to fore/back interpolation (`p430,130=137,69,35`, `p500,150=128,58,29`, `p520,225=104,31,16`) and the focused WASM metrics improved to `exactMismatch=185964`, `meanDelta=7.941013658776962`, `badFraction=0.0321457536786004`, but the target samples are `160,75,38`, `144,62,31`, and `255,222,111`. The first sample exactly equals the old bgColor-multiplied pixel (`56,44,22`) plus `foreColor=0x681f10`, and the second is within one rounding step of the same formula. Implementation step: indexed `DARKEN` sprite rendering now keeps the existing bgColor multiply and adds the sprite `foreColor` as an RGB offset instead of interpolating between `foreColor` and `backColor`; the focused `ScriptModifiedBitmapTest` class still passes with this additive behavior.
- Full-frame exact audit after the additive `DARKEN` correction: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_EXACT_POLLS=80 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-current-after-additive --no-daemon` still failed with `exactMismatch=176715`, `meanDelta=7.52343619253528`, and `badFraction=0.03249526899161936`. The key warm photo samples now match or nearly match (`p430,130=160,75,38` exact; `p500,150=144,63,32` vs target `144,62,31`), but the full frame differs because the harness clicked while the avatar was still in the doorway with the yellow tile highlight active. The target frame has the avatar settled inside the room and includes the selected-photo info/action UI, including the `Pick up` action. This confirms the final gate must drive the same full-frame room/UI state, not just the photo bitmap.
- Harness timing correction in progress: `roomEnterTestV31ClickPhoto` now waits `LS_V31_ROOM_PHOTO_PRE_CLICK_SETTLE_MS` (default `15000`) after the entered-room checkpoint before clicking the wall photo. This is a harness timing correction so the click happens after the normal avatar/object state settles toward the full-frame reference; it does not alter runtime behavior or the v31 loader/networking parameters.
- Current diagnostic output directories: `/opt/git/LibreShockwave/build/v31-photo-palette-info-diagnostic`, `/opt/git/LibreShockwave/build/v31-photo-postclick-sprite-diagnostic`, and `/opt/git/LibreShockwave/build/v31-photo-sample-index-diagnostic`. Temporary diagnostics added for this stage must be removed before final verification: `CastLibManager` media info/sample logs and the post-click failure sprite dumps in the JS harness.
- Remaining verification gap: the active goal is not complete until the opened-photo render is exact (`exactMismatch=0`) against `/opt/git/v31_room_load/v31_native_rooms_entered_clicked_photo.png`, no script error remains, and the combined v31/v14/v1 WASM gate passes.

## Required loader (v31 path)
Use this loader block **verbatim** for the V31 flow:

```javascript
 var betaClientParams = {"sw1":"client.allow.cross.domain=1;client.notify.cross.domain=0","sw2":"connection.info.host=verysecret.classichabbo.com;connection.info.port=30100","sw3":"connection.mus.host=verysecret.classichabbo.com;connection.mus.port=39101","sw4":"site.url=;url.prefix=","sw5":"client.reload.url=/client/beta?x=reauthenticate;client.fatal.error.url=/clientutils?key=error","sw6":"client.connection.failed.url=/clientutils?key=connection_failed;external.variables.txt=https://images.classichabbo.com/gamedata/external_variables.txt?","sw7":"external.texts.txt=https://images.classichabbo.com/gamedata/external_texts.txt?","sw8":"use.sso.ticket=1;sso.ticket=vibe-sso-admin-504d3ba4-acdb-4436-b67c-d0752f44f767","sw9":"forward.type=2;forward.id=1"};
```

Important:
- Use exactly this ordering and values.
- `sw9` is required so the client forwards directly into the room flow (`forward.type=2;forward.id=1`).
- `connection.mus.host` and `connection.mus.port` are already correct for this client:
  - `connection.mus.host=verysecret.classichabbo.com`
  - `connection.mus.port=39101`
- Do not alter websocket/networking behavior.

## Reference assets

- V31 room flow checkpoints in `/opt/git/v31_room_load/`:
  - `v31_native.png`
  - `v31_native_rooms.png`
  - `v31_native_rooms_entered.png`
  - `v31_native_rooms_entered_clicked_photo.png`
- Baseline comparison anchor directory (immutable): `/opt/git/v14_v31_compare/`
  - Use `v14_native.png` and `v1_native.png` as non-regression visual anchors.

## Repro steps to validate (v31, WASM test)
1. Enter via v31 loader using the exact `betaClientParams` above.
2. Do not require or assert `/opt/git/v31_room_load/v31_native.png` in `roomEnterTestV31`.
3. Do not click `Go`; `sw9=forward.type=2;forward.id=1` should forward straight into the room.
4. Poll room load and confirm `/opt/git/v31_room_load/v31_native_rooms_entered.png` before clicking the photo.
5. Click the photo furni item on the wall.
6. Confirm the photo opens and the opened-photo render matches `/opt/git/v31_room_load/v31_native_rooms_entered_clicked_photo.png`.

## Verification (no scope expansion)
- Keep v31 websocket setup unchanged while debugging:
  - `connection.mus.host=verysecret.classichabbo.com`
  - `connection.mus.port=39101`
- Do not regress v14/v1 behavior while implementing the v31 fix.
- Any v14 or v1 visual/test regression blocks the v31 fix, even if the v31 room-photo path passes.
- Add/extend the v31 room-photo test flow in WASM so it verifies this exact sequence.
- This test must run via `player-wasm` and this file:
  - `player-wasm/src/test/js/wasm-v31-room-photo-hotspot-native-test.js`
- The active v31 hotspot test inside that file must be named `roomEnterTestV31ClickPhoto`.
- `roomEnterTestV31` must be available separately in `player-wasm/src/test/js/wasm-v31-room-enter-native-test.js`, but it is not the active test when validating the photo hotspot.
- This must be a new/updated WASM regression test file (JS), not `.java`.

Use this one-pass command (WASM path + offline movie-load non-regression suite):

```bash
./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest :player-wasm:runWasmNativeVisualRegressionTest --no-daemon
```
Note: if the first target image is delayed, only tune harness timing first (no code changes yet), in this order:
1. Raise `LS_V31_ROOM_PHOTO_MAX_POLLS` above default 1200.
2. Raise `LS_V31_ROOM_PHOTO_POLL_MS` above default 500.
3. Raise `LS_V31_ROOM_PHOTO_SETTLE_MS` above default 1200.

If the first reference image is slow to stabilize, increase the room-photo harness timeout before changing code:
- Increase polls first:
  - `LS_V31_ROOM_PHOTO_MAX_POLLS` (default 1200)
  - `LS_V31_ROOM_PHOTO_POLL_MS` (default 500)
- If first reference is still missing after timeout tuning, increase settle delay:
  - `LS_V31_ROOM_PHOTO_SETTLE_MS` (default 1200)
- Example:

```bash
LS_V31_ROOM_PHOTO_MAX_POLLS=1800 LS_V31_ROOM_PHOTO_POLL_MS=700 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest --no-daemon
```

Existing non-regression test for v14/v1 (reuse as-is):
- `./gradlew :player-wasm:runWasmNativeVisualRegressionTest`
- This test must load the movies and compare the initial native visual states only.
- This test must not require connecting to any host/port, posting data, SSO login, navigator flow, room entry, TCP proxy, or MUS proxy for the v1/v14 checks.
- Use `/opt/git/v14_v31_compare/` as the visual anchor directory for the v31/v14/v1 comparison assets.

Treat these as hard gates in the same command context:
- `./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest :player-wasm:runWasmNativeVisualRegressionTest --no-daemon`

If the offline v14/v1 movie-load visual regression fails, stop and treat it as a regression before broadening changes.

## Latest full-frame exact audit
- The final target is now explicitly full-frame exact: the complete `959x540` output must match `/opt/git/v31_room_load/v31_native_rooms_entered_clicked_photo.png`, including dropdowns, menus, the bottom bar, room chrome, and every visible UI element.
- Current focused exact run: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_EXACT_POLLS=80 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-current-after-additive --no-daemon`.
- Current exact result: `exactMismatch=176715`, `meanDelta=7.52343619253528`, `badFraction=0.03249526899161936`, `exactMatchFraction=0.6587591240875912`.
- Important split in the failure: the real Venus photo now loads and upper photo samples are correct or nearly correct (`p430,130` exact `160,75,38`; `p500,150` off by one), but the full frame still differs because room/user/UI state is not the target state.
- The most visible full-frame blocker is avatar/object state: actual sprites still show the avatar at the doorway (`ch=96/ch=97 loc=279,88`) while the reference has the avatar inside the room near center. The bottom bar/right action UI and menus also differ, so a region-only photo match is not acceptable.
- Timing check: adding `LS_V31_ROOM_PHOTO_PRE_CLICK_SETTLE_MS=15000` before clicking the photo did not change the exact result (`exactMismatch=176715`) and did not move the avatar out of the doorway. The active investigation is therefore not a simple "clicked too early" timing issue.
- Default-settle confirmation: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_EXACT_POLLS=80 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-default-settle --no-daemon` failed with the same stable metrics: `exactMismatch=176715`, `meanDelta=7.52343619253528`, `badFraction=0.03249526899161936`, `exactMatchFraction=0.6587591240875912`, `spriteCount=70`.
- Default-settle artifact: `/opt/git/LibreShockwave/build/v31-photo-default-settle/roomEnterTestV31ClickPhoto_photo_click_attempt_0_last.png` shows the real Venus photo rendered in the photo window and the selected-photo info/action UI, including the bottom-right photo panel and `Pick up` action. The blocker is still the whole-frame state: doorway/avatar/room chrome and all surrounding UI must match the reference exactly, not merely the photo bitmap.
- Trace-harness update: `wasm-v31-room-photo-hotspot-native-test.js` can now install trace handlers through `LS_TRACE_HANDLERS`.
- Bounded status trace: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_TRACE_HANDLERS=handle_status,handle_users,action_mv,Refresh,resetValues LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_MAX_POLLS=90 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=0 LS_V31_ROOM_PHOTO_PRE_CLICK_SETTLE_MS=0 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-room-status-trace-bounded --no-daemon` failed early as expected at the room-enter visual wait (`lastMean=94.45181323125095`, `lastBad=0.6415961842969142`).
- Bounded status trace limitation: that short run only captured the initial room MUS socket open/hello/DU exchange and one entry visualizer `Refresh`; it did not reach useful `handle_users`, `handle_status`, `resetValues`, or `action_mv` traces before timeout. The next useful trace must run long enough to hydrate room status while still writing artifacts reliably.
- Full status trace result: a longer `LS_TRACE_HANDLERS=handle_status,handle_users,action_mv,Refresh,resetValues` run showed `handle_users` placing `Quackster` at the doorway, followed by `handle_status` calling `resetValues(3, 5, 0.0, ..., [])` and `Refresh(3, 5, 0.0)`. No `action_mv` was received. This means the avatar is not failing to interpolate; the server/client state being consumed by the normal room handler has the controlled user at the doorway unless a real `MOVE` command is sent.
- Lingo audit: `hh_human/...Human Class EX.ls` sets `pScreenLoc` directly in `resetValues` and only interpolates during `action_mv`; `hh_room/...Room Handler Class.ls` parses server status actions and only calls movement handlers when the action list contains them; `hh_photo/...Photo Item Class.ls` selects/opens the photo without moving the avatar. Therefore photo selection is not expected to change avatar location.
- Diagnostic harness update: `roomEnterTestV31ClickPhoto` now has env-gated pre-photo floor-click controls (`LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_X`, `LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_Y`, `LS_V31_ROOM_PHOTO_PRE_MOVE_SETTLE_MS`) so this state mismatch can be tested without changing the default flow.
- Diagnostic floor-click run: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_X=410 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_Y=225 LS_V31_ROOM_PHOTO_PRE_MOVE_SETTLE_MS=7000 LS_V31_ROOM_PHOTO_PRE_CLICK_SETTLE_MS=0 LS_V31_ROOM_PHOTO_EXACT_POLLS=40 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-pre-move-410-225 --no-daemon` still failed exact, but moved the avatar close to the reference position and slightly improved the final metrics to `exactMismatch=175121`, `meanDelta=7.277450791073873`, `badFraction=0.03009693739620747`, `exactMatchFraction=0.6618371760707528`.
- Diagnostic floor-click sprite dump: after the move, the avatar shadow was at `loc=390,226` and avatar sprites were at `loc=375,136`, while the room still rendered `leftdoor_open` / `leftdoor_open_mask` at channels `89`/`90`. The remaining large mismatch is therefore not only avatar position; door/room chrome and the full surrounding UI still differ from the reference.
- Door-state audit: `/opt/git/v31_assets/hh_room_private/text/01551_model_s.room_stxt_0.txt` defines the room door as static visualizer elements `leftdoor_open` and `leftdoor_open_mask`, with `leftdoor_open` also active as `command: GOAWAY`. This is the room layout's default door visual, not a teleporter `DoorIn`/`DoorOut` object. The next root-cause investigation must explain how the target full-frame state gets a different door/room visual state, or whether the exact harness still starts from the wrong server/user room state.
- Wrapper color audit: temporary `VizWrap_` sampling showed floor/wall wrapper tinting is active and stable. At final frame sample points, `VizWrap_floor01` bakes floor pixels as `0x458bb7`, `VizWrap_wall01` bakes `p160,245` as `0xa5a8b7`, and `VizWrap_wall02` bakes `p472,10` as `0x6d6e77`. The target floor sample at `p100,320` is darker (`0x366e92`) than the current baked floor (`0x458bb7`), so the largest remaining room-area mismatch is not explained by missing floor/wall wrapper creation.
- Room-shadow lead: the same wrapper audit shows `VizWrap_roomShadow` exists in the failing frame at channel `94`, `loc=0,0`, `960x540`, `ink=41`, `blend=30`, but its dynamic bitmap is fully transparent (`alpha=0-0`, `transparent=518400`) and samples bake as white/no-op. The target room floor looks like the current room after an additional darkening pass, making `roomShadow` population/rendering the strongest current root-cause lead.
- Lingo source audit for the shadow path: `Room Component Class.roomConnected` defines `roomShadow` through `Shadow Manager.define("roomShadow")`; `Shadow Manager.addShadow` appends object shadow parts to that wrapper and switches the wrapper ink to `36`; `Active Object Class.solveMembers` calls `tShadowManager.addShadow(tProps)` for private-room active object shadows, then `tShadowManager.render()`. Therefore the next runtime investigation should focus on active object validation/creation and why no object shadow parts survive into `VizWrap_roomShadow`.
- Broad shadow trace result: `LS_TRACE_HANDLERS=createActiveObject,solveMembers,addShadow,removeShadow,render,updateWrap,addPart,renderImage` with the diagnostic floor click still failed exact with the same improved metrics (`exactMismatch=175121`, `meanDelta=7.277450791073873`, `badFraction=0.03009693739620747`). It also slowed room loading and produced excessive unrelated `render`/`PropList` noise, while the saved sprite dump again showed `VizWrap_roomShadow` fully transparent. Future tracing should be narrower and avoid generic `render`.
- Diagnostic cleanup: the temporary `VizWrap_` sampler in `WasmEntry.appendCataloguePixelSamples` was removed after recording the wrapper samples above. Do not reintroduce broad permanent wrapper sampling; add any future probes behind a focused env gate or keep them local-only.
- Roomdimmer trace correction: a first trace with `LS_TRACE_HANDLERS=handleDimmerPresets,roomdimmerDefined,setState,update,applyEffect,renderRoomBackground,renderWrappedParts,setRoomDimmerColor` was stopped because the generic `update` handler traced unrelated per-frame components and distorted room-entry timing. Do not use bare `update` in future trace lists.
- Clean roomdimmer trace: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_TRACE_HANDLERS=handleDimmerPresets,roomdimmerDefined,applyEffect,renderRoomBackground,renderWrappedParts,setRoomDimmerColor LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_X=410 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_Y=225 LS_V31_ROOM_PHOTO_PRE_MOVE_SETTLE_MS=7000 LS_V31_ROOM_PHOTO_PRE_CLICK_SETTLE_MS=0 LS_V31_ROOM_PHOTO_EXACT_POLLS=5 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-roomdimmer-trace-clean --no-daemon` reached the real Venus photo path and failed with the same diagnostic pre-move final metrics (`exactMismatch=175121`, `meanDelta=7.277450791073873`, `badFraction=0.03009693739620747`).
- Clean roomdimmer trace finding: filtering `/opt/git/LibreShockwave/build/v31-roomdimmer-trace-clean/browser_console.log` found no `handleDimmerPresets`, `roomdimmerDefined`, `applyEffect`, `renderRoomBackground`, `renderWrappedParts`, `setRoomDimmerColor`, `roomdimmer_set_state`, `colorizeRoom`, `setDimmerColor`, or packet `365` hits. The entered-room and post-click sprite dumps also show the wall photo and room wrappers, but no obvious roomdimmer wall-item sprite. This makes an active roomdimmer/colorize-room effect unlikely in the current failing room state.
- Venus roomdimmer source audit: `../Venus/FEATURES.md` correctly documents `MSG_ROOMDIMMER_GET_PRESETS (341)`, `MSG_ROOMDIMMER_SET_PRESET (342)`, `MSG_ROOMDIMMER_CHANGE_STATE (343)`, and `DimmerPresets (365)`. Venus routes those packets in `RoomRouteRegistrar` and replies from `RoomHandlers.handleRoomDimmerGetPresets` via `RoomResponseWriter.sendDimmerPresets`.
- Venus behavior detail: Venus does not send dimmer presets proactively on room entry; it sends packet `365` only after the client sends `MSG_ROOMDIMMER_GET_PRESETS`. The v31 client sends that request from `Roomdimmer Component Class.roomdimmerDefined`, which only runs when `Furniture Roomdimmer Class.define` executes for an actual roomdimmer wall item. Therefore the missing dimmer traces most likely mean the active room currently has no roomdimmer object in its room item stream, not that packet `365` decoding alone is broken.
- Live Venus data limitation: local MariaDB is running, but only the `vibe` schema is present and it has no `items` table; the configured `venus` game DB is absent locally. The active sockets also are not local listeners (`verysecret.classichabbo.com:30100/39101`), so the exact live room-1 item rows and moodlight preset rows could not be inspected from this workspace. If a roomdimmer is expected for the reference state, the next investigation needs access to the actual running Venus game database or server logs to confirm room 1's placed wall items.
- Full-frame requirement reminder from the latest user direction: the final photo output must be pixel-perfect for the whole captured frame, including dropdowns, menus, bottom bar, room chrome, right panel, and any transient UI. Passing only the opened photo bitmap or a cropped room region is not acceptable.
- Latest region audit against `/opt/git/v31_room_load/v31_native_rooms_entered_clicked_photo.png` using the clean diagnostic pre-move artifact `/opt/git/LibreShockwave/build/v31-roomdimmer-trace-clean/roomEnterTestV31ClickPhoto_photo_click_attempt_0_last.png`: full frame `AE=175121`, `MAE=1402.73`; room shell `AE=156751`, `MAE=1416.78`; photo window `AE=15337`, `MAE=3166.14`; photo image `AE=8937`, `MAE=3889.27`; bottom bar `AE=10006`, `MAE=3727.55`; chat input `AE=2112`, `MAE=4550.57`; `Say` dropdown `AE=907`, `MAE=11798.5`; right panel `AE=2072`, `MAE=2909.19`; `Pick up` button `AE=603`, `MAE=15009`; avatar area `AE=5975`, `MAE=2646.74`; door area `AE=4287`, `MAE=1449.76`.
- Representative pixel samples from that same run: floor/wrapper samples are not uniformly darkened. Some room pixels are exact or off by one (`p160,245 target=165,168,182 actual=165,168,183`; `p95,512` and `p915,512` exact), while floor samples alternate between target and actual pattern colors (`p100,320 target=54,110,146 actual=69,139,183`; `p500,350 target=69,139,182 actual=55,111,145`). The `Pick up`/right-panel area still has hard UI differences (`p920,477 target=239,239,239 actual=0,0,0`).
- Strongest new visual lead: rolling only the actual floor crop (`720x310+40+180`) by `+0+1` reduced mean error from `MAE=2317.04 (0.0353558)` to `MAE=247.251 (0.00377281)`, even though exact mismatch remained high because edges, avatar, and UI are still present in the crop. This indicates a one-pixel floor texture phase/origin error, not a simple missing roomdimmer/darkening pass.
- Lingo wrapper audit for this lead: `Visualizer Instance Class.buildVisual` creates wrappers with `#offsetx=pLocX` and `#offsety=pLocY`; `Visualizer Part Wrapper Class.renderImage` bakes each part into a full-stage image at `tPart[#locH] - tPartMem.regPoint[1]`, `tPart[#locV] - tPartMem.regPoint[2]`; `updateSprite` then places the wrapper sprite at `point(pOffsets[1], pOffsets[2])`. The current clean sprite dump shows `VizWrap_floor01` at `loc=32,0`, `960x540`, so the wrapper offset path is active and stable.
- Java registration hypothesis to prove next: score-sprite rendering in `StageRenderer.scaledRegPoint` uses `BitmapInfo.regXLocal()/regYLocal()` for bitmap members, and media loading sets bitmap anchors to those local values, but `CastMember.getBitmapProp("regpoint")` currently returns the stored `regPointX/regPointY` copied from `CastMemberChunk`. If `member.regPoint` in Lingo should expose bitmap-local registration for authored bitmaps, wrapper-baked visuals can be one pixel out of phase even when normal score sprites render with the local registration. This must be verified with a targeted runtime probe or focused unit test before changing shared registration semantics.
- Registration hypothesis result: a temporary `WasmEntry` named-bitmap diagnostic was added, run, and removed. It reported `flat_floor_0_a_0_0_0 member=22:42 infoPal=68 reg=0,0 infoReg=0,0 localReg=0,0 initial=0,0`; `left_wallpart_0_a_0_0_0 reg=-32,115 infoReg=-32,115 localReg=-32,115 initial=0,0`; `right_wallpart_0_a_0_2_0 reg=0,116 infoReg=0,116 localReg=0,116 initial=0,0`; and both door bitmaps `reg=-10,79 infoReg=-10,79 localReg=-10,79 initial=0,0`. Therefore the floor/wall/door wrapper phase mismatch is **not** caused by raw-vs-local bitmap registration for these room assets.
- Probe-run caution: `/opt/git/LibreShockwave/build/v31-regpoint-probe/roomEnterTestV31ClickPhoto_photo_click_attempt_0_last.png` is useful for metadata only. It did not fully open/load the photo window with `LS_V31_ROOM_PHOTO_EXACT_POLLS=5`; photo samples were white (`p430,130`, `p500,150`, `p520,225` all actual `255,255,255`), so its worse visual metrics (`exactMismatch=184789`, `meanDelta=11.0866`, `badFraction=0.0618951`) should not replace the clean pre-move visual baseline.
- Narrow room-shadow trace: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_TRACE_HANDLERS=createActiveObject,addShadow,removeShadow LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=0 LS_V31_ROOM_PHOTO_PRE_CLICK_SETTLE_MS=0 LS_V31_ROOM_PHOTO_MAX_POLLS=260 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-shadow-trace-narrow --no-daemon` reached the entered-room reference and then failed intentionally before photo clicks. Filtering `browser_console.log` and `roomEnterTestV31_window_sprites.txt` found no `createActiveObject`, `addShadow`, or `removeShadow` trace entries; `VizWrap_roomShadow` still existed at `ch=94`, `loc=0,0`, `ink=41`, `blend=30`, with `alpha=0-0` and `transparent=518400`.
- Shadow-trace interpretation: the focused trace makes a shared `Shadow Manager.addShadow` rendering bug less likely for this state. The more likely issue is that the live room state being consumed during the run has no shadow-bearing private-room active objects reaching `Active Object Class.solveMembers`, or active object creation is happening through a differently named path that still leaves `roomShadow` empty. The next useful investigation should compare the live room item/object stream against the target screenshot state and confirm whether the reference was captured with additional placed active objects or a different room/user state.
- Live room object/item handler map: in `/opt/git/v31_assets/projectorrays_lingo/hh_room/casts/External/ParentScript 5 - Room Handler Class.ls`, packet `30` maps to `handle_OBJECTS` and validates legacy/passive floor objects, packet `32` maps to `handle_activeobjects`, packets `93`/`94`/`95` map to active-object add/remove/update, packet `45` maps to `handle_itemlist`, and packets `83`/`84`/`85` map to wall-item add/remove/update. `ParentScript 4 - Room Component Class.ls` then creates those through `createPassiveObject`, `createActiveObject`, and `createItemObject` once the room cache process is complete.
- Focused object-stream trace: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_TRACE_HANDLERS=handle_activeobjects,handle_activeobject_add,handle_activeobject_update,handle_itemlist,handle_additem,handle_updateitem,createActiveObject,createPassiveObject,createItemObject LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=0 LS_V31_ROOM_PHOTO_PRE_CLICK_SETTLE_MS=0 LS_V31_ROOM_PHOTO_MAX_POLLS=260 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-object-stream-trace --no-daemon` reached the entered-room reference and failed intentionally before photo clicks.
- Object-stream trace finding: filtering `/opt/git/LibreShockwave/build/v31-object-stream-trace/browser_console.log` found exactly one traced room-object handler call, `handle_activeobjects([#subject: 32, #content: "H", ...])`, and no `createActiveObject`, `createPassiveObject`, `createItemObject`, `handle_itemlist`, `handle_additem`, or update-handler traces. The corresponding sprite dump contains static room wrappers, `leftdoor_open`, `leftdoor_open_mask`, one `leftwall photo` sprite, the user/avatar sprites, room info text, and bottom-bar UI, but still no active/passive furniture sprites and no populated `roomShadow`.
- Object-stream interpretation: the current live room state appears to have no shadow-bearing active floor objects in packet `32` and no additional visible object/item stream that would explain the target's full-frame differences. This weakens the room-shadow/object-population lead and shifts the next investigation back to runtime rendering/UI state differences that are already visible without extra room objects: the one-pixel floor texture phase, avatar/user selection state, right action panel, dropdown/menu state, and bottom bar must all match the reference exactly.
- Full-frame exactness requirement is now explicit from user direction: the final photo output must be pixel-perfect over the whole screenshot, including dropdowns, menus, bottom bar, right panel, room chrome, room art, and transient UI. The acceptance gate must be `exactMismatch=0`; threshold passes, crop-only matches, or photo-window-only matches are not sufficient.
- Cross-region shift analysis against the clean diagnostic pre-move artifact `/opt/git/LibreShockwave/build/v31-roomdimmer-trace-clean/roomEnterTestV31ClickPhoto_photo_click_attempt_0_last.png` shows the room-authored visual content is consistently one pixel too high. Best overlap is `dx=0 dy=1` for the floor crop `720x310+40+180` (`MAE=185.71`, `0.00283375`), left wall `360x230+90+170` (`MAE=166.653`, `0.00254296`), right wall `360x230+300+170` (`MAE=301.601`, `0.00460213`), door `90x150+270+80` (`MAE=237.292`, `0.00362085`), and photo region `230x220+390+70` (`MAE=420.656`, `0.0064188`). This is not floor-only and is not explained by avatar movement, roomdimmer, or shadow population alone.
- Target dimension correction for future comparisons: the active reference image `/opt/git/v31_room_load/v31_native_rooms_entered_clicked_photo.png` is `959x540` in the image tools and harness, despite earlier wording that called it `960x540`. Exact matching must use the actual `959x540` frame size.
- Anchor/matte metadata probe result: a temporary `WasmEntry` diagnostic was added, run, and removed. It reported the floor matte as a normal indexed white matte (`size=130x71`, `depth=8`, `pal=Custom Palette`, `nativeAlpha=false`, `hasNativeMatteAlpha=false`, `matteIndex=0`, `matteRgb=ffffff`, `opaque=5006`, `transparent=4224`, `edgeOpaque=2,2,7,7`). This weakens the theory that the one-pixel room phase comes from matte polarity or flood-fill edge handling.
- Runtime sprite metadata from the same temporary probe showed dynamic wrapper anchors are not inheriting a bad offset: `VizWrap_floor01` had `castReg=0,0`, `castLocal=0,0`, `castInitial=0,0`, `dynReg=0,0`, `dynAnchor=0,0`; authored visible bitmaps had zero initial rects; `leftdoor_open`/`leftdoor_open_mask` used `castReg=-10,79`, `castLocal=-10,79`, `castInitial=0,0`; and the wall photo used `castReg=7,11`, `castLocal=7,11`, `castInitial=0,0`. This makes raw-vs-local registration and wrapper dynamic-anchor drift unlikely for the current 1px vertical error.
- Current root-cause lead after these probes: the remaining full-frame mismatch likely includes a room visualizer, stage coordinate, or authored-bitmap vertical origin semantic error that shifts room/authored content by one pixel while UI surfaces still need their own exact-state audit. A speculative global off-by-one registration change must not be committed without proving it improves the exact frame and does not regress the v1/v14 offline visual gates.
- Fresh exact run before the renderer-origin correction: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_EXACT_POLLS=80 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-fresh-exact --no-daemon` failed with the stable default metrics `exactMismatch=176715`, `meanDelta=7.52343619253528`, `badFraction=0.03249526899161936`, `exactMatchFraction=0.6587591240875912`.
- Whole-frame shift experiment: pasting the fresh actual frame down by one pixel in image analysis reduced the post-click metrics to `exactMismatch=156242`, `meanRGB=1.2540602994374284`, and `badFraction=0.011198007183408644`. A temporary software-compositor `+1` Y shift produced the same post-click runtime metric (`exactMismatch=156242`, `meanDelta=1.2540602994357295`, `badFraction=0.006851272544703201`), and the existing v1/v14 offline visual regression still passed under `LS_NATIVE_VISUAL_CASES=v1,v14`.
- Renderer-origin implementation step: the one-pixel vertical raster correction now lives in `StageRenderer` as `DIRECTOR_SPRITE_RASTER_Y_ORIGIN`, applied when constructing render sprites instead of as a post-blit WASM/compositor shift. This keeps sprite diagnostics and all render backends aligned with the corrected Director raster origin. `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH ./gradlew :player-core:compileJava :player-wasm:compileJava --no-daemon` passes.
- StageRenderer validation evidence: with `LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_X=410`, `LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_Y=225`, `LS_V31_ROOM_PHOTO_PRE_MOVE_SETTLE_MS=7000`, `LS_V31_ROOM_PHOTO_PRE_CLICK_SETTLE_MS=0`, `LS_V31_ROOM_PHOTO_EXACT_POLLS=30`, and one photo click, `/opt/git/LibreShockwave/build/v31-photo-stage-y-plus-one-premove` still failed exact but improved to `exactMismatch=153931`, `meanDelta=0.9106315735274985`, and `badFraction=0.003937357586992624`. The same StageRenderer change passed `LS_NATIVE_VISUAL_CASES=v1,v14 ./gradlew :player-wasm:runWasmNativeVisualRegressionTest -PoutputDir=/opt/git/LibreShockwave/build/native-visual-stage-y-plus-one --no-daemon`.
- Remaining exactness profile after the StageRenderer origin fix plus diagnostic avatar move: `153931` pixels still differ, but most are one-channel or one-unit RGB differences (`(0,0,1)` for `81385` pixels, `(1,1,-1)` for `39410`, `(1,1,1)` for `21993`). Only `3790` pixels have RGB sum-absolute delta greater than `3`, and only `3350` exceed `30`. Larger residual errors are concentrated in the speech dropdown/menu, right action panel, bottom bar, photo image/window, and avatar/selection state; the next investigation should target exact UI state and Director color/ink rounding, not revisit MUS/photo transport.
- Post-photo mouse-state diagnostic: the photo harness now has env-gated neutral mouse controls (`LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_X`, `LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_Y`, `LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_SETTLE_MS`) so hover/tooltip state can be tested without changing the default flow. With the StageRenderer fix plus diagnostic avatar pre-move and `LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_X=300`, `LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_Y=20`, `/opt/git/LibreShockwave/build/v31-photo-stage-y-plus-one-premove-mouse-300-20` still failed exact but improved slightly to `exactMismatch=153743`, `meanDelta=0.8550876041149159`, and `badFraction=0.0035743251071718224`. The visible bottom-bar tooltip is cleared in the crop, but the `Pick up` action/right-panel state and subtle full-frame color/raster differences remain non-exact.
- Named dynamic-member compatibility check: Director's `createMember("common.button.text", #text)` path is now implemented and unit-tested because the common button class uses a named shared text member while composing action buttons. The focused run `/opt/git/LibreShockwave/build/v31-photo-named-create-member-mouse-300-20` was unchanged (`exactMismatch=153743`, `meanDelta=0.8550876041149159`, `badFraction=0.0035743251071718224`), and the enlarged crop still shows the actual `pick.button` with the hand icon but no `Pick up` label. This fixes a real missing Director API but rules it out as the current cause of the right-panel action-label mismatch.
- Right-panel text source audit: `obj_disp_actions_furni.window` defines `pick.button` with `#key:"pickup"`, `#model:5`, and `#icon:"pick/wave_button_icon"`. The live Classichabbo external texts fetched by the harness include `pickup=Pick up`, `move=Move`, `rotate=Rotate`, and `delete=Delete`, and the harness logs confirm it pre-fetched `https://images.classichabbo.com/gamedata/external_texts.txt?`. Therefore the missing `Pick up` label is not caused by absent localization in the current LibreShockwave test run.
- Venus localization investigation: `../Venus/venus-server/src/main/resources/bootstrap/havana/havana.sql` includes `('pickup', 'Pick up')`, but `../Venus/venus-web/src/main/java/org/oldskooler/venus/web/feature/gamedata/controller/GamedataController.java` and `../Venus/venus-web/src/main/java/org/oldskooler/venus/web/app/route/PublicRoutes.java` expose only furnidata/productdata routes, while `WebSettingCatalog` still defaults `external.texts.txt` to `/gamedata/external_texts.txt?`. This is a real Venus web-client serving gap to track separately, but it is not the active cause of the current LibreShockwave harness missing-label frame because the harness uses and receives the Classichabbo external text file.
- Action-button trace finding: `LS_TRACE_HANDLERS=createButtonImg,setButtonImage,setText` in `/opt/git/LibreShockwave/build/v31-photo-button-trace` logged `setText("Photo from Habbo")` and empty text wrapper calls, but did not log `createButtonImg` or `setButtonImage` for the `pick.button`. The sprite dump still contains `obj.disp.actions_pick.button` as a `69x17` dynamic bitmap with the hand icon but no text render. This suggests the action button is being rendered as a base/grouped window element without running the expected Common/Icon Button behavior, or that the model-specific button class is not being resolved for `window.button5.class`.
- Button-class lookup context: v31 `System Props` contains `window.button5.class = ["Common Button Class","Icon Button Class"]`, and `Window Instance Class.CreateElement` should compute `window.button5.class` from `#type:"button"` and `#model:5` before creating the element object. The next targeted step is to prove whether `variableExists/getClassVariable("window.button5.class")` succeeds and whether `prepare` dispatches into `Common Button Class`/`Icon Button Class`; the previous broad `variableExists,prepare` trace was too noisy and did not show a clean button-class decision.
- Narrow create-element trace limitation: `LS_TRACE_HANDLERS=CreateElement,getClassVariable` in `/opt/git/LibreShockwave/build/v31-photo-createelement-trace` eventually reached the same exact mismatch (`exactMismatch=153743`, `meanDelta=0.8550876041149159`, `badFraction=0.0035743251071718224`) but the committed prop-list diagnostic made the run excessively noisy and distorted early room timing. Filtering the saved log found `obj.disp.actions` manager lookups but still no clean `window.button5.class`, `pick.button`, or `CreateElement(...#type:"button"...#model:5)` trace lines, so this run should be treated as non-conclusive for the button factory branch.
- Photo binary sanity check from the same noisy trace: the Venus/SMUS photo payload is arriving and checksum-valid (`BINARYDATA image bytes=15572`, `serverCs=78151`, `localCs=78151`, `size=161x117`). The remaining `153743` exact mismatch is therefore not a regression back to the earlier invalid-photo or missing-binary path.
- Diagnostic cleanup: the always-on `[PropList]` binary lookup logger in `PropListMethodDispatcher` was removed after it served its purpose. It produced very large stderr output, distorted room timing during traced WASM runs, and was not acceptable as permanent runtime behavior. `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH ./gradlew :vm:compileJava :player-wasm:compileJava --no-daemon` passes after the cleanup.
- Right-panel crop audit: comparing `120x80+840+440` crops from the target and `/opt/git/LibreShockwave/build/v31-photo-stage-y-plus-one-premove-mouse-300-20/roomEnterTestV31ClickPhoto_photo_click_attempt_0_last.png` shows the target has the full `Pick up` label, while the actual frame shows the hand icon/button pixels but not the label. The crop pair is saved under `/opt/git/LibreShockwave/build/v31-crops/actions-target-actual.png`.
- Clean button trace result: `LS_TRACE_HANDLERS=createButtonImg,setButtonImage,setText,getText,CreateElement,getClassVariable` in `/opt/git/LibreShockwave/build/v31-photo-button-trace-clean` still failed exact at the same metric (`exactMismatch=153743`, `meanDelta=0.8550876041149159`, `badFraction=0.0035743251071718224`) but proved that the right-panel factory path is healthy: `CreateElement` builds `pick.button` with `#model:5` and `#key:"pickup"`, `getClassVariable("window.button5.class")` resolves, `Icon Button Class.prepare` runs, `getText("pickup")` is called, and `createButtonImg("Pick up", #up)` is invoked. Therefore the missing right-panel label is no longer a class-lookup/localization bug; the next root-cause target is image/text composition or later bitmap publication for the icon button.
- Clean button trace photo sanity check: the same run again received a checksum-valid Venus photo payload (`BINARYDATA image bytes=15572`, `serverCs=78151`, `localCs=78151`, `size=161x117`) and ended at the same `153743` residual mismatch. This reinforces that the remaining exactness work is full-frame raster/UI exactness, including the button label, dropdown/menu/bottom-bar states, and subtle color/raster deltas.
- Focused Java text-rendering check: `CastMemberTextImageTest.iconButtonTextMeasurementKeepsFullGlyphHeight` reproduces the `Icon Button Class` setup for `Pick up` (`font="vb"`, size `9`, `#adjust`, fixed line space `11`) and proves the generic text renderer can produce a multi-pixel-height label with visible black glyphs. The missing `Pick up` label is therefore not explained by isolated text measurement/rendering alone.
- Button bitmap probe result: `/opt/git/LibreShockwave/build/v31-photo-button-bitmap-probe/roomEnterTestV31ClickPhoto_photo_click_attempt_0_window_sprites.txt` shows `obj.disp.actions_pick.button` as a `69x17` dynamic bitmap with `black=307`, `nonWhite=745`, and full `bounds=0,0,68,16`, while the direct `icon.button.text` bitmap probe reports `size=0x0`. This run used only one exact poll and captured before the real photo binary arrived, so its visual metrics are not the current baseline; its useful finding is that the published button bitmap is non-empty but still lacks the target text label. The next diagnostic should probe the text member via its rendered `image` property and member text/rect/font props, not only `member.getBitmap()`.
- Rendered text-member probe result: `/opt/git/LibreShockwave/build/v31-photo-rendered-text-probe/roomEnterTestV31ClickPhoto_photo_click_attempt_0_window_sprites.txt` now probes `icon.button.text` through `member.image`. The shared scratch text member is healthy at capture time (`text="Delete"`, `rect=rect(0, 0, 36, 11)`, `height=11`, `renderedSize=40x11`, `black=122`, `bounds=0,2,35,8`). Because this member is reused by the action buttons, it being `Delete` at capture time is expected and does not explain the earlier `Pick up` composition failure. The actual right-panel crop from this run still shows the target `Pick up` label on the left crop and only the icon/horizontal rule on the actual crop (`/opt/git/LibreShockwave/build/v31-crops-live/target-vs-rendered-text-probe-actions.png`).
- Focused copy-path check: `CastMemberTextImageTest.iconButtonTextCopyIntoPalettedImageKeepsGlyphPixels` reproduces copying the black-on-white `Pick up` text image into an 8-bit paletted button image without an explicit `#ink` prop, matching `Icon Button Class.createButtonImg`. It passes, so the missing label is not currently explained by generic text rendering, `member.image`, or raw `copyPixels` from a 32-bit text image into an 8-bit button canvas.
- Failed rerun note: `/opt/git/LibreShockwave/build/v31-photo-action-buttons-probe` is not useful for diagnostics because the DCR fetch hit a transient CORS/fetch failure before the movie loaded, then was manually killed after the harness kept polling with zero sprites. Do not treat that run as visual evidence.
- Button copyPixels probe result: `/opt/git/LibreShockwave/build/v31-photo-button-copy-probe/roomEnterTestV31ClickPhoto_photo_click_attempt_0_window_sprites.txt` proves `Icon Button Class.createButtonImg` copies the `Pick up` glyphs into the dynamic `69x17` paletted `obj.disp.actions_pick.button` image. The text copy line reports `src=46x11x32`, `srcBlack=126`, `afterBlack=126`, and the final dynamic member has `black=307`. The missing visible label was therefore not class lookup, localization, text rendering, or buffer publication.
- Root-cause refinement for the right-panel label: the copied 32-bit text image had an opaque exact-white background. When that exact white reached the paletted button image, the sprite-level `BACKGROUND_TRANSPARENT` ink keyed it out, leaving black glyphs over the black button interior. Sampling the reference crop showed the visible label background is `#EFEFEF`, not exact white, so the correct runtime path is palette quantization for RGB `copyPixels` into indexed UI images while keeping palette slot `0` exact white available as the matte key.
- Rejected experiment: a broad final-sprite flood-fill matte for script-modified indexed `BACKGROUND_TRANSPARENT` sprites preserved some white button chrome and worsened the frame to `exactMismatch=170354`, so it was removed. The fix belongs in the image copy/palette path, not in final sprite baking.
- Current button-label fix: `ImageMethodDispatcher.refreshDestinationPaletteIndices` now refreshes both palette indices and RGB pixels after RGB-to-indexed `copyPixels`, and avoids consuming matte-white palette slot `0` for copied exact-white RGB when a visible near-white entry is available. `ScriptModifiedBitmapTest.rgbCopyIntoPalettedImageKeepsMatteWhiteSlotAvailable` covers the root case.
- Validation after the palette-copy fix: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_X=410 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_Y=225 LS_V31_ROOM_PHOTO_PRE_MOVE_SETTLE_MS=7000 LS_V31_ROOM_PHOTO_PRE_CLICK_SETTLE_MS=0 LS_V31_ROOM_PHOTO_EXACT_POLLS=10 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_X=300 LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_Y=20 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-rgb-copy-palette-white-polls10 --no-daemon` still fails the required exact gate, but reaches the Venus photo payload and improves the stable metric to `exactMismatch=153373`, `mean=0.6837903423067093`, `bad=0.002855984242845557`. The right-panel `Pick up` label is now visible; the saved crop is `/opt/git/LibreShockwave/build/v31-crops-live/rgb-copy-palette-actions-6x.png`.
- Regression gate after the palette-copy fix: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_NATIVE_VISUAL_CASES=v1,v14 ./gradlew :player-wasm:runWasmNativeVisualRegressionTest -PoutputDir=/opt/git/LibreShockwave/build/native-visual-rgb-copy-palette-white --no-daemon` passed.
- Director DARKEN rounding root cause: sprite-level `InkProcessor.multiplyColor` used `/255` per-channel multiplication, while Director-compatible `copyPixels` DARKEN behavior and the remaining color histogram pointed to fixed-point `/256` truncation with an exact-white special case. Examples from the residual frame matched this: tint `(70,141,185)` with gray `253` should produce `(69,139,182)` instead of `(69,139,183)`, and tint `(172,175,190)` with gray `246` should produce `(165,168,182)` instead of `(165,168,183)`.
- DARKEN fix: sprite-level DARKEN now uses Director fixed-point channel multiplication, returning the tint exactly for source channel `255` and otherwise using `(src * tint) >> 8`. This preserves existing white-pixel behavior while fixing the off-by-one palette colors that dominated the previous full-frame residual.
- Focused DARKEN validation: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH ./gradlew :player-core:test --tests com.libreshockwave.player.render.pipeline.InkProcessorTest.darkenKeepsOpaqueWhitePixelsFor32BitBitmapWithoutNativeAlpha --tests com.libreshockwave.player.ScriptModifiedBitmapTest.darkenCopyPixelsTintsGrayscaleSourceByDirectorShadeMath --no-daemon` passed.
- Comparable WASM photo validation after the DARKEN fix: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_X=410 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_Y=225 LS_V31_ROOM_PHOTO_PRE_MOVE_SETTLE_MS=7000 LS_V31_ROOM_PHOTO_PRE_CLICK_SETTLE_MS=0 LS_V31_ROOM_PHOTO_EXACT_POLLS=120 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_X=300 LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_Y=20 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-darken-256-polls120 --no-daemon` still fails the required exact gate, but the binary server path is valid in this run (`HELLO`, `GETBINDATA`, checksum-valid `BINARYDATA`, `size=161x117`) and the stable post-photo metric improves to `exactMismatch=62864`, `mean=0.5469933958981708`, `bad=0.002855984242845557`.
- Regression gate after the DARKEN fix: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_NATIVE_VISUAL_CASES=v1,v14 ./gradlew :player-wasm:runWasmNativeVisualRegressionTest -PoutputDir=/opt/git/LibreShockwave/build/native-visual-darken-256 --no-daemon` passed.
- Remaining exactness profile after the DARKEN fix: the output is still not pixel-perfect. Region counts from `/opt/git/LibreShockwave/build/v31-photo-darken-256-polls120/roomEnterTestV31ClickPhoto_photo_click_attempt_0_last.png` are `full ae=62864 big=3232`, `right ae=241 big=241`, `photoWin ae=9389 big=103`, `photoImg ae=4477 big=0`, `bottom ae=1770 big=1770`, `say ae=152 big=152`, `avatar ae=9222 big=69`, and `room ae=59627 big=1205`. The dominant remaining deltas are still tiny channel differences (`(0,0,-1)` for `41278`, `(-1,0,0)` for `14165`, `(0,0,-2)` for `4179`), with the larger residual concentrated in the bottom bar, speech dropdown/menu, right action panel, avatar/selection state, and photo window chrome. The acceptance gate remains `exactMismatch=0` over the full `959x540` frame.
- Rejected copyPixels DARKEN experiment: unifying `ImageMethodDispatcher.copyPixels` DARKEN math with the sprite-level DARKEN helper passed focused Java coverage, but regressed the comparable v31 photo run. With the same diagnostic pre-move and neutral-mouse settings, `/opt/git/LibreShockwave/build/v31-photo-copypixels-darken-director` failed with `exactMismatch=103913`, `mean=0.574088234914818`, and `bad=0.002855984242845557`, worse than the current `62864` baseline. The experiment was reverted. The remaining tiny color deltas are therefore not solved by a broad VM copyPixels DARKEN channel-rule change.
- Full-tint DARKEN channel fix: the largest post-DARKEN photo-image residual was caused by sprite `ink=41` with `back=ffcc66`. The red tint channel is `255`, and Director preserves source values for full-intensity tint channels while still using fixed-point `/256` for partial tint channels. LibreShockwave was converting all non-white red values to `source - 1`, creating thousands of `(-1,0,0)` deltas inside the `161x117` photo member. `InkProcessor.multiplyDirectorChannel` now treats `tint == 255` as identity before applying the partial-channel fixed-point multiply.
- Focused full-tint validation: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH ./gradlew :player-core:test --tests com.libreshockwave.player.render.pipeline.InkProcessorTest.darkenKeepsOpaqueWhitePixelsFor32BitBitmapWithoutNativeAlpha --tests com.libreshockwave.player.render.pipeline.InkProcessorTest.darkenPreservesChannelsWithFullTint --tests com.libreshockwave.player.ScriptModifiedBitmapTest.darkenCopyPixelsTintsGrayscaleSourceByDirectorShadeMath --no-daemon` passed.
- Comparable WASM photo validation after the full-tint fix: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_X=410 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_Y=225 LS_V31_ROOM_PHOTO_PRE_MOVE_SETTLE_MS=7000 LS_V31_ROOM_PHOTO_PRE_CLICK_SETTLE_MS=0 LS_V31_ROOM_PHOTO_EXACT_POLLS=120 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_X=300 LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_Y=20 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-darken-full-tint-channel --no-daemon` still fails the exact gate, but the stable post-photo metric improves to `exactMismatch=48943`, `mean=0.5380926633964856`, `bad=0.002855984242845557`; the binary photo payload remains checksum-valid.
- Regression gate after the full-tint fix: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_NATIVE_VISUAL_CASES=v1,v14 ./gradlew :player-wasm:runWasmNativeVisualRegressionTest -PoutputDir=/opt/git/LibreShockwave/build/native-visual-darken-full-tint-channel --no-daemon` passed.
- Remaining exactness profile after the full-tint fix: photo-image red-channel mismatches are largely gone (`photo_image ae=622`, with the remaining photo-image differences mostly hard edge/stroke/window overlap pixels). The next dominant class is room wrapper color: `ref (54,110,146)` vs `actual (54,110,145)` for `39410` pixels and `ref (43,88,117)` vs `actual (43,88,115)` for `2081` pixels. A temporary `VizWrap_` sampler, run and removed, showed for example `VizWrap_floor01` raw `dyn=ffc9c9c9` baking to `ff366e91` at stage point `(100,320)` while the target is `ff366e92`, and `VizWrap_wall02` raw `dyn=ff949494` baking to `ff6c6e77` while target samples are one blue channel higher. The next investigation should focus on the room visualizer wrapper source/color semantics, not the photo transport or right-panel button path.
- CopyPixels room-wrapper color fix: `ImageMethodDispatcher.copyPixels` DARKEN remapping now preserves subtle RGB channel bias for non-indexed grayscale-classified sources during `#bgColor` tinting, while indexed sources still use palette-index shade semantics. This matches the `VizWrap_floor01`/`VizWrap_wall02` evidence where wrapper source values that should carry a small blue bias were collapsing to neutral grayscale before sprite DARKEN baked the room colors.
- Focused copyPixels validation: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH ./gradlew :player-core:test --tests com.libreshockwave.player.ScriptModifiedBitmapTest.darkenCopyPixelsTintsGrayscaleSourceByDirectorShadeMath --tests com.libreshockwave.player.ScriptModifiedBitmapTest.darkenCopyPixelsPreservesSubtleSourceColorBias --tests com.libreshockwave.player.ScriptModifiedBitmapTest.maskedDarkenCopyPixelsUsesPaletteColorShadeForCustomIndexedSource --tests com.libreshockwave.player.render.pipeline.InkProcessorTest.darkenPreservesChannelsWithFullTint --no-daemon` passed.
- Comparable WASM photo validation after the copyPixels channel-bias fix: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_X=410 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_Y=225 LS_V31_ROOM_PHOTO_PRE_MOVE_SETTLE_MS=7000 LS_V31_ROOM_PHOTO_PRE_CLICK_SETTLE_MS=0 LS_V31_ROOM_PHOTO_EXACT_POLLS=120 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_X=300 LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_Y=20 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-copypixels-preserve-channel-bias --no-daemon` still fails the required exact gate, but the checksum-valid Venus photo path reaches a stable `exactMismatch=3486`, `mean=0.5061882876968009`, `bad=0.002855984242845557`, improving the previous `48943` full-tint baseline by removing most remaining room-wrapper color collapse. The full-frame gate remains `exactMismatch=0` over the whole `959x540` final photo frame, including dropdowns, menus, bottom bar, room chrome, right panel, and all transient UI.
- Regression gate after the copyPixels channel-bias fix: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_NATIVE_VISUAL_CASES=v1,v14 ./gradlew :player-wasm:runWasmNativeVisualRegressionTest -PoutputDir=/opt/git/LibreShockwave/build/native-visual-copypixels-preserve-channel-bias --no-daemon` passed.
- Remaining exactness profile after the copyPixels channel-bias fix: the full-frame mismatch is down to `3486` pixels. Region counts from `/opt/git/LibreShockwave/build/v31-photo-copypixels-preserve-channel-bias/roomEnterTestV31ClickPhoto_photo_click_attempt_0_last.png` are `photoWin ae=1009 big=755`, `photoImg ae=622 big=612`, `bottom ae=1767 big=1767`, `say ae=105 big=105`, `right ae=606 big=606`, `pickup ae=0`, `avatar ae=0`, `room ae=1481 big=1227`, and `door ae=118 big=118`. The dominant remaining pairs are hard UI/state or edge/chrome deltas rather than broad room tint drift: `ref #efefef -> actual #555555` for `680` pixels in the Say/dropdown/bottom UI area, `ref #eeeeee -> actual #fdfdfd` for `472` pixels, `ref #ffcc99 -> actual #a0a0a4` for `367` bottom-bar pixels, and several photo-image/window edge color swaps around `x=390..577, y=120..243`. The next target is exact UI/window/photo-edge state, not MUS transport, photo payload validity, or room wrapper color.
- Say control root cause and fix: the bottom-left `Say` control reproduced the same indexed-UI matte class as the action buttons through a different path. The control is composed into a `66x20` paletted bitmap, then a 32-bit black/gray-on-exact-white text image is copied over it with plain `COPY`. Treating copied text-image exact-white pixels as ordinary opaque white destroyed the prebuilt indexed button fill; when the final sprite used background-transparent ink, those exact-white pixels keyed out and exposed toolbar gray behind the `Say` label. `ImageMethodDispatcher.copyPixels` now treats exact-white background pixels in opaque grayscale text images copied into indexed destinations as transparent, preserving the existing control fill while copying the glyph pixels.
- Focused text-background validation: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH ./gradlew :player-core:test --tests com.libreshockwave.player.ScriptModifiedBitmapTest.rgbCopyIntoPalettedImageKeepsMatteWhiteSlotAvailable --tests com.libreshockwave.player.ScriptModifiedBitmapTest.rgbTextCopyIntoPalettedControlPreservesExistingWhiteButtonFill --tests com.libreshockwave.player.ScriptModifiedBitmapTest.darkenCopyPixelsPreservesSubtleSourceColorBias --no-daemon` passed.
- Comparable WASM photo validation after the text-background fix: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_X=410 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_Y=225 LS_V31_ROOM_PHOTO_PRE_MOVE_SETTLE_MS=7000 LS_V31_ROOM_PHOTO_PRE_CLICK_SETTLE_MS=0 LS_V31_ROOM_PHOTO_MAX_POLLS=360 LS_V31_ROOM_PHOTO_EXACT_POLLS=120 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_X=300 LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_Y=20 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-text-white-preserve-rerun --no-daemon` still fails the required exact gate, but improves to `exactMismatch=2725`, `mean=0.2896220342692309`, `bad=0.0013903371567605145`; the Venus photo remains checksum-valid. The `Say` region drops from `105` mismatching pixels to `5`, and the bottom region drops from `1767` to `1012`.
- Regression gate after the text-background fix: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_NATIVE_VISUAL_CASES=v1,v14 ./gradlew :player-wasm:runWasmNativeVisualRegressionTest -PoutputDir=/opt/git/LibreShockwave/build/native-visual-text-white-preserve --no-daemon` passed.
- Remaining exactness profile after the text-background fix: full-frame `exactMismatch=2725`. Region counts are `photoWin ae=1009 big=755`, `photoImg ae=622 big=612`, `bottom ae=1012 big=1012`, `say ae=5 big=5`, `right ae=600 big=600`, `pickup ae=0`, `avatar ae=0`, `room ae=1481 big=1227`, and `door ae=118 big=118`. The dominant remaining pairs are now `ref #eeeeee -> actual #fdfdfd` for `472` pixels around the room info/text area, bottom icon/status colors such as `ref #ffcc99 -> actual #a0a0a4` for `367` pixels and `ref #333333 -> actual #33333b` for `349` pixels, and photo/window edge swaps around `x=390..577, y=120..243`. The next investigation should target photo window/image edge compositing and remaining bottom/status UI color state.
- Photo media matte investigation: the checksum-valid Venus photo payload is assigned via `member.media` as 8-bit Director bitmap media (`size=161x117`, `bitDepth=8`, `paletteId=-3`) and then drawn with sprite `ink=41`. The decoded media itself is a full opaque rectangle; the remaining photo cutouts came from applying indexed flood-fill matte inference during DARKEN. This is incorrect for rectangular media payloads assigned from Director `media`, because their edge-connected dark palette values are real image content, not authored transparent matte pixels.
- Rectangular-media DARKEN fix: `Bitmap` now carries a `rectangularMedia` metadata flag, `CastMember.setMediaProp(Datum.Media)` sets it for decoded bitmap media assignments, derived bitmaps preserve it, and `InkProcessor.resolveMatteSpec` skips DARKEN/LIGHTEN flood-fill matte inference for that specific media class. `InkProcessorTest.darkenKeepsIndexedRectangularMediaEdgesOpaque` covers the root case.
- Focused rectangular-media validation: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH ./gradlew :player-core:test --tests com.libreshockwave.player.render.pipeline.InkProcessorTest.darkenKeepsIndexedRectangularMediaEdgesOpaque --tests com.libreshockwave.player.render.pipeline.InkProcessorTest.darkenPreservesChannelsWithFullTint --tests com.libreshockwave.player.ScriptModifiedBitmapTest.rgbCopyIntoPalettedImageKeepsMatteWhiteSlotAvailable --tests com.libreshockwave.player.ScriptModifiedBitmapTest.rgbTextCopyIntoPalettedControlPreservesExistingWhiteButtonFill --tests com.libreshockwave.player.ScriptModifiedBitmapTest.darkenCopyPixelsPreservesSubtleSourceColorBias --no-daemon` passed.
- Comparable WASM photo validation after the rectangular-media fix: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_V31_ROOM_PHOTO_SKIP_LEGACY_VISUALS=1 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_X=410 LS_V31_ROOM_PHOTO_PRE_MOVE_CLICK_Y=225 LS_V31_ROOM_PHOTO_PRE_MOVE_SETTLE_MS=7000 LS_V31_ROOM_PHOTO_PRE_CLICK_SETTLE_MS=0 LS_V31_ROOM_PHOTO_MAX_POLLS=360 LS_V31_ROOM_PHOTO_EXACT_POLLS=120 LS_V31_ROOM_PHOTO_MAX_CLICK_ATTEMPTS=1 LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_X=300 LS_V31_ROOM_PHOTO_POST_CLICK_MOUSE_Y=20 ./gradlew :player-wasm:runWasmV31RoomPhotoHotspotTest -PoutputDir=/opt/git/LibreShockwave/build/v31-photo-rectangular-media-darken --no-daemon` still fails the required full-frame exact gate, but improves to `exactMismatch=2654`, `mean=0.2615616833378324`, `bad=0.001253234464913297`; the Venus `BINARYDATA` path remains checksum-valid (`serverCs=78151`, `localCs=78151`, `size=161x117`).
- Regression gate after the rectangular-media fix: `JAVA_HOME=/opt/jdk-21.0.11+10 PATH=/opt/jdk-21.0.11+10/bin:$PATH LS_NATIVE_VISUAL_CASES=v1,v14 ./gradlew :player-wasm:runWasmNativeVisualRegressionTest -PoutputDir=/opt/git/LibreShockwave/build/native-visual-rectangular-media-darken --no-daemon` passed.
- Remaining exactness profile after the rectangular-media fix: full-frame `exactMismatch=2654`. Region counts are `photoWin ae=938 big=684`, `photoImg ae=551 big=541`, `bottom ae=1012 big=1012`, `say ae=52 big=52`, `right ae=726 big=726`, `room ae=1454 big=1200`, and `door ae=118 big=118`. The photo placement still has best overlap at `dx=0, dy=0`; the next targets are exact photo/window edge compositing, bottom bar/icon status colors, right-panel/dropdown state, and the room-info/text `#eeeeee -> #fdfdfd` residual. Acceptance remains `exactMismatch=0` over the whole `959x540` final frame.

## Acceptance criteria
- V31 flow enters the room, clicks the photo furni item on the wall, opens the photo, and renders `/opt/git/v31_room_load/v31_native_rooms_entered_clicked_photo.png` consistently.
- The final opened-photo output is pixel-perfect for the full frame, including dropdowns, menus, bottom bar, room chrome, and all UI outside the photo itself; cropped or region-only photo matches are not sufficient.
- `roomEnterTestV31ClickPhoto` starts from the entered-room screen and validates the post-click pixel diff against the opened-photo reference.
- `roomEnterTestV31ClickPhoto` saves the post-click actual render and diff metadata in the output directory.
- v14 and v1 offline movie-load visual regressions stay green in the same verification pass.
- The fix is implemented as a root-cause runtime/object-interaction correction, not special-case behavior.
- The implementation contains no `Player.java` room-visualizer state repair loop, no client shim, and no test-only path that makes production behavior pass by accident.
- The final change identifies the corrected root cause in code review notes or commit/PR text before it is accepted.
