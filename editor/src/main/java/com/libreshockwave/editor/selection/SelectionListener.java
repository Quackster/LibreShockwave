package com.libreshockwave.editor.selection;

/**
 * Observer interface for selection changes in the editor.
 */
public interface SelectionListener {
    void selectionChanged(SelectionEvent event);
}
